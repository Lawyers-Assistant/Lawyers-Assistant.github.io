{"quickstart": {"t": "Quickstart — run the desktop app", "b": "Clone → install → run → first query → verify", "x": "quickstart — run the desktop app this guide walks a new developer from a fresh clone to asking their first legal question in the desktop app. (the root \u0001 is the non-technical guide for end users — see the launcher at \u0001 / \u0001.) prefer watching? \u0001 does everything: it starts the python backend (auto-creating the venv), the vite dev server, and the electron window. the only manual step is installing node dependencies. --- 1. prerequisites tool why version ------ ----- --------- python backend + models (electron creates the \u0001 for you, but python 3 must be on path) 3.11+ node.js vite, react build, electron 18+ (npm 9+) git clone the repo any gpu (optional) gpu-accelerated embedding (falls back to cpu) nvidia + cuda for llm-powered answers you also need ollama running locally (\u0001) with a model pulled (the launcher's default is \u0001). if ollama isn't running, retrieval still works; answers report the model as \"unavailable\". 2. clone & install python is handled automatically. on first \u0001, the electron main process (\u0001) checks for \u0001; if missing it creates it with system python and installs \u0001. it falls back to system python if venv creation fails. 3. run the app from the repo root, one command starts the whole desktop app: \u0001 launches electron only — and electron boots everything else itself: process who starts it port --------- --------------- ------ backend electron's python bridge (\u0001) — uses \u0001 via the venv manager, runs \u0001 8765 frontend electron's vite launcher (\u0001) 5173 electron \u0001 (the single process you start) — (loads the vite url) why only electron? the electron main process already spawns the backend and vite itself (see \u0001). a \u0001 triple-start would launch duplicates that fight over ports 8765/5173. need just the backend or frontend for browser-only dev? use the piecemeal scripts instead: \u0001, \u0001, or \u0001 (backend + frontend, no electron window). 1. electron kills any stale processes on ports 8765/5173. 2. it spawns the python backend and polls \u0001 until ready (30s timeout, 500ms interval). 3. it starts vite and opens the electron window. 4. the app's bootloader runs its own health check; the ui appears when the backend responds \u0001. first launch is slower: creating the venv + installing python deps happens once. the backend also warms up models (bge-m3 embedder, bge-reranker, ollama, classifier references) in a background thread at startup, so the first query is fast after that. 4. add documents (ingestion) two ways: a. in the app (per-file, easy) 1. click the + (attach) button in the chat input bar — or drag & drop a file (pdf, image, txt, etc.) onto the input area. 2. pick a project folder first (the \"work in a folder\" button) — chat, upload and scan all require an active workspace; there is no default \u0001 folder anymore. 3. send a message. the file is uploaded to \u0001 (saved into the project folder itself), ingested into chromadb, and indexed for bm25. 4. attach files without typing and it runs a default batch-scan prompt (\"scan these documents for compliance issues: …\"). up to 5 files per message (or a whole folder — up to 50 supported docs — via the folder picker in the input bar). b. bulk initial ingest (cli, one-time corpus) with the app's venv active (or your own): pipeline: collect chunks (docling parse + structure-aware chunking) → embed with bge-m3 → save to chromadb → build the bm25 index (\u0001 does all four phases). 5. ask your first question type a legal question in the input bar, e.g.: what happens (visible live in the ui): 1. the intent router classifies the message and writes a plan. 2. the agent calls \u0001 (dense + sparse + rerank) and \u0001. 3. retrieved chunks are cited inline; the answer streams in with chain-of-thought, tool cards, and statuses in the message. 4. the sources panel lists cited documents (file, page, section, score); click one to open the source viewer. the pipeline editor (pipeline button in the sources header) lights up the active node live. 6. compliance scan (playbook) open the scan panel, pick a document + playbook, and run a scan. chunks are classified by clause type and checked against the playbook rules; findings (flags) appear with severity and can be resolved / dismissed / escalated. see \u0001 §6. 7. verify it works 8. troubleshooting (quick) symptom fix --------- ----- boot screen stuck on \"loading\" backend didn't start — check the electron console; ensure python 3 is on path \"could not reach the backend server\" port 8765 blocked/stale process — restart \u0001 (it kills stale 8765/5173 listeners) answers say model \"unavailable\" ollama isn't running or has no model — start ollama, \u0001 empty results corpus not ingested — run \u0001 or attach files in the app gpu out of memory \u0001 stuck on something else? the full guide — \u0001 — covers 12 failure scenarios with diagnostics commands (netstat/lsof, curl health checks, ollama checks). 9. next steps - troubleshooting → \u0001 - architecture → \u0001 - backend/api/agents → \u0001 - frontend/ui/stores → \u0001 - pipeline editor → \u0001 - terminology → \u0001 - agent q&a → \u0001"}, "development": {"t": "Development — Workflows, Debugging & Tools", "b": "Dev environment, hot reload, debugging, profiling", "x": "development — workflows, debugging & tools developer guide for working on lawyer assistant. covers development environment setup, debugging strategies, hot reload, database inspection, and performance profiling. --- 1. quick start for developers what \u0001 does: 1. starts electron main process 2. electron spawns python backend (port 8765) 3. electron spawns vite dev server (port 5173) 4. electron loads vite url in browserwindow --- 2. development modes mode 1: full app (electron) use when: - testing electron-specific features (ipc, native dialogs) - end-to-end testing - building/packaging logs: - backend logs → electron console (stdout/stderr piped) - frontend logs → electron devtools (cmd+option+i / ctrl+shift+i) mode 2: web-only (browser) starts backend + frontend in parallel (no electron). use when: - frontend-only work (faster iteration) - backend api testing (via browser devtools network tab) - css/ui tweaks access: \u0001 limitations: - no native file dialogs (falls back to \u0001) - no electron ipc (workspace picker limited) mode 3: backend-only starts only the fastapi server (port 8765). use when: - backend-only work - api testing with \u0001/postman - running benchmarks test: mode 4: frontend-only (vite) starts only the vite dev server (requires backend running separately). use when: - ui iteration (fastest hot reload) - component development --- 3. hot reload behavior frontend (vite + react fast refresh) hot module replacement (hmr): - edit a \u0001 file → component re-renders without page reload - state is preserved (zustand store not reset) - sub-second latency full reload triggers: - \u0001 changes - \u0001 changes - new dependencies added test hmr: save → page updates instantly without reload. backend (manual restart) python backend does not auto-reload — restart manually: windows: macos/linux: why no auto-reload: - model loading is expensive (5-10s warmup) - chromadb connections need clean shutdown - electron manages backend lifecycle workaround for rapid backend iteration: now backend auto-reloads on python file changes. --- 4. debugging strategies debugging frontend (react) react devtools: 1. open electron app 2. press \u0001 (mac) or \u0001 (windows) 3. install react devtools extension (if in browser) zustand state inspection: install redux devtools extension → see zustand state live. console logging: network inspection: - devtools → network tab - filter by \u0001 - inspect sse streams (eventstream) debugging backend (python) print debugging: structured logging: set log level: python debugger (pdb): run backend manually (not via electron): when breakpoint hits: vs code debugging: \u0001: set breakpoints in vs code → f5 to start debugging. debugging electron main process debugging: \u0001: logs appear in the terminal that ran \u0001. renderer process debugging: same as frontend debugging (devtools). ipc debugging: --- 5. database inspection chromadb (vector store) list collections: collection stats: query vectors: peek at stored chunks: sqlite (conversations, flags) langgraph conversations: compliance flags: workspace history (per-project) file: \u0001 structure: --- 6. performance profiling backend profiling cprofile (python built-in): analyze: line profiler: run: gpu profiling pytorch profiler: nvidia-smi monitoring: frontend profiling react devtools profiler: 1. open devtools → profiler tab 2. click ⏺️ record 3. interact with app 4. stop recording 5. inspect flame graph (which components re-rendered) chrome devtools performance: 1. devtools → performance tab 2. record → interact → stop 3. see javascript execution, rendering, painting --- 7. log file locations log location purpose ----- ---------- --------- backend \u0001 rotating file (10mb, 5 backups) launcher \u0001 app launch logs ollama serve \u0001 ollama daemon logs electron console terminal stdout/stderr backend stdout piped to electron tail logs: search logs: --- 8. cli scripts for development located in \u0001: \u0001 — bulk ingestion \u0001 — cli search \u0001 — cli chat \u0001 — rebuild bm25 rebuilds \u0001 from chromadb chunks. \u0001 — a/b test compares two retrieval configs side-by-side. --- 9. testing during development run tests on file change (watch mode) run specific tests frontend type checking lint --- 10. common development tasks add a new api endpoint 1. backend: \u0001 2. frontend: \u0001 3. ui: \u0001 add a new environment variable 1. backend: \u0001 2. usage: add a new test 1. create test file: \u0001 2. run: --- 11. troubleshooting development issues issue cause fix ------- ------- ----- port 8765 busy stale backend process kill python, restart vite hmr not working browser cache hard refresh (ctrl+shift+r) backend not starting python not on path check \u0001 models not loading missing \u0001 dir run launcher setup frontend build fails node modules stale \u0001 type errors typescript strict mode fix types or add \u0001 tests fail chromadb path conflict use \u0001 fixture --- 12. development best practices code style python (backend): - pep 8 style guide - type hints for function signatures - docstrings for public functions typescript (frontend): - functional components (react hooks) - props interfaces for components - \u0001/\u0001 over \u0001 chains git workflow before committing --- 13. related documentation doc coverage ----- ---------- \u0001 test suite, fixtures, running tests \u0001 performance benchmarks \u0001 common failures \u0001 system overview \u0001 api reference \u0001 ui architecture \u0001 all env vars & settings"}, "testing": {"t": "Testing — Test Suites, Benchmarks & Quality Assurance", "b": "Test suite, fixtures, running the tests", "x": "testing — test suites, benchmarks & quality assurance comprehensive testing guide for lawyer assistant. the test suite uses a phase-based organization that mirrors the actual system architecture, making it easy to identify which layer a failure affects. --- 1. test suite organization tests live in \u0001 and are organized by system phase : phase-based testing philosophy each phase tests a single layer of the stack: 1. phase 1 (boot) — can we start? config loading, logging, directory creation 2. phase 2 (types) — are data structures valid? pydantic model validation 3. phase 3 (chunking) — can we split documents? chunk size, overlap, section respect 4. phase 4 (embeddings) — can we embed text? bge-m3 inference, normalization 5. phase 5 (vector store) — can we store/retrieve vectors? chromadb operations 6. phase 6-8 (retrieval) — can we search? dense, sparse, hybrid, reranking 7. integration — does the full pipeline work end-to-end? this makes debugging trivial: a phase 3 failure means chunking broke, not embeddings. --- 2. running tests run all tests run a specific phase run a specific test function run with coverage coverage report → \u0001 run only fast tests (skip slow integration) run integration tests only --- 3. test environment setup tests use in-memory chromadb and mock embeddings by default (fast, no gpu needed): for tests that need real models (integration), use the \u0001 marker: run gpu tests separately: --- 4. phase 1: boot & configuration file: \u0001 tests system startup essentials: what's tested ✅ configuration loading - default values from \u0001 - environment variable overrides (\u0001) - config singleton behavior ✅ logging setup - log level from \u0001 - file handler creation (\u0001) - rotating file handler (10mb, 5 backups) ✅ directory creation - \u0001 creates all required directories - workspace directory creation ✅ device resolution - \u0001 → cuda mps cpu - respects explicit device override example tests --- 5. phase 2: data model validation file: \u0001 tests all pydantic models for structural integrity: what's tested ✅ request/response models (\u0001) - \u0001 validation - \u0001 structure - \u0001 types ✅ document models - \u0001 (file name, page, section, text) - \u0001 (chunk id, metadata) ✅ configuration dataclasses - \u0001, \u0001, \u0001 - frozen attributes (immutability) example tests --- 6. phase 3: chunking file: \u0001 tests the \u0001: what's tested ✅ section respect - chunks never split across section boundaries - headers stay with their content ✅ chunk size targets - chunks stay near \u0001 (480 tokens default) - minimum chunk size enforced (32 tokens) ✅ overlap handling - overlap tokens correctly copied between chunks - no duplicate text when overlap=0 ✅ edge cases - single-word documents - empty sections - very long sections (must split eventually) example tests --- 7. phase 4: embeddings file: \u0001 tests bge-m3 embedding generation: what's tested ✅ embedding shape - output is 1024-dim for bge-m3 - l2-normalized (unit vectors) ✅ batch processing - batch embedding produces same results as single - batch size respected ✅ semantic similarity - similar texts have high cosine similarity - unrelated texts have low similarity ✅ device handling - respects \u0001 config (cuda/cpu) - falls back gracefully when gpu unavailable example tests --- 8. phase 5: vector store file: \u0001 tests chromadb operations via \u0001: what's tested ✅ add/retrieve chunks - \u0001 stores vectors + metadata - \u0001 retrieves top-k by cosine similarity ✅ metadata filtering - filter by \u0001, \u0001, \u0001 - combined filters work correctly ✅ deduplication - upserting same chunk id updates, doesn't duplicate - collection size remains correct ✅ persistence - chromadb survives restart (persistentclient) - collection metadata persists example tests --- 9. phase 6-8: retrieval & reranking file: \u0001 tests the full retrieval pipeline (\u0001): what's tested ✅ dense retrieval (chromadb) - returns top-k by cosine similarity - respects \u0001 parameter ✅ sparse retrieval (bm25) - keyword matching works - bm25 scores are reasonable ✅ hybrid merge strategies - \u0001 — union of dense + sparse - \u0001 — reciprocal rank fusion - \u0001 — dense results, bm25 boosts scores ✅ reranking (bge-reranker-v2-m3) - cross-encoder re-scores candidates - top-k after rerank differs from pre-rerank - rerank scores in [0, 1] ✅ skip rerank - \u0001 bypasses reranker - falls back to retrieval scores example tests --- 10. integration: end-to-end rag file: \u0001 tests the complete rag flow from query to answer: what's tested ✅ full pipeline - ingest document → query → retrieve → rerank → answer - all components integrated ✅ streaming chat - sse events stream correctly - \u0001 event contains answer + sources ✅ tool execution - \u0001 tool works - \u0001 tool works - \u0001 tool works ✅ citation generation - answer includes \u0001, \u0001 markers - citations match returned sources example tests --- 11. gpu manager tests file: \u0001 (37 tests) tests smart gpu resource management ( no gpu required — pure logic tests): what's tested ✅ registration - models register with priority, vram estimate, promote/demote functions ✅ promotion/demotion - \u0001 promotes model to vram - models demote per residency mode (eager/adaptive/persistent) ✅ priority eviction - lower-priority models evicted first under pressure - higher-priority models never evicted for lower ones ✅ hotness tracking - models become \"hot\" after \u0001 within \u0001 - old uses expire from sliding window ✅ residency modes - \u0001 — demote always - \u0001 — keep hot models, evict cold - \u0001 — keep all ✅ auto-batch tuning - \u0001 scales with free vram - never exceeds configured cap - cpu fallback keeps configured batch ✅ ollama eviction - \u0001 called under vram pressure when enabled example tests --- 12. pipeline config tests file: \u0001 tests runtime pipeline reconfiguration (\u0001): what's tested ✅ node presence → config derivation - rerank node present → \u0001 - rerank node absent → \u0001 - dense+sparse → \u0001 - dense only → \u0001 ✅ validation - unknown node types dropped - dangling edges dropped - duplicate ids deduplicated ✅ query overrides - \u0001 wins over layout-derived value - \u0001 overrides fusion weights example tests --- 13. workspace tests file: \u0001 tests workspace management and folder watcher: what's tested ✅ workspace lifecycle - \u0001 → storage paths resolve under workspace - \u0001 → falls back to project \u0001 ✅ folder fingerprinting - fingerprint captures (size, mtime) per file - excludes workspace storage dirs - excludes temp/lock files ✅ auto-ingestion - watcher detects new files - ingests only new files (skips already-indexed) - serializes with manual ingestion via \u0001 ✅ watcher status - \u0001 returns thread-safe snapshot - last scan timestamp, files found/indexed, chunks example tests --- 14. other test files \u0001 tests context hint retrieval (native right-click → exact chunk citation): - extracts chunk from user-selected text - exact match in vector store - fallback to semantic search when exact match fails \u0001 tests citation integrity: - citation markers \u0001 validated against source count - malformed markers stripped (\u0001 when only 5 sources) - lexical entailment check (chunk supports claim) \u0001 tests citation string parsing: - \u0001 → \u0001 - multiple citations extracted - malformed citations handled gracefully \u0001 tests chain-of-thought extraction: - \u0001 blocks extracted - post-tool reasoning segments detected - adaptive reasoning window (short segments only) \u0001 tests abstain signal and temperature control: - \u0001 when pipeline abstains - temperature parameter respected in llm calls \u0001 tests post-tool reasoning gate: - untagged text after tool result treated as cot - max segments and char limits enforced - long segments (answer body) excluded from cot \u0001 tests cloud api provider integration: - openai-compatible provider configuration - api key validation - model list fetching - embedding/chat api calls --- 15. test fixtures common test fixtures in \u0001 (or inline): --- 16. running tests in ci github actions workflow (\u0001): --- 17. test coverage goals target coverage by module: module target status -------- -------- -------- \u0001 90% ✅ \u0001 85% ✅ \u0001 90% ✅ \u0001 85% ✅ \u0001 80% ✅ \u0001 95% ✅ (37 tests) \u0001 70% ⚠️ (partial) \u0001 75% ⚠️ (partial) \u0001 60% ⚠️ (needs more) --- 18. writing new tests test naming convention examples: - \u0001 - \u0001 - \u0001 test structure (arrange-act-assert) use descriptive assertion messages parametrize for multiple cases --- 19. troubleshooting test failures chromadb permission errors cause: chromadb client not closed between tests fix: use \u0001 fixture for isolated dbs: cuda out of memory in tests cause: real models loaded in tests fix: use mocks or skip gpu tests: run without gpu tests: slow test suite cause: integration tests run on every invocation fix: mark slow tests: run fast tests only: --- 20. related documentation doc coverage ----- ---------- \u0001 performance benchmarks, golden eval \u0001 dev environment, debugging \u0001 api endpoints, agents, tools \u0001 gpu manager design & tests \u0001 workspace watcher tests"}, "benchmarking": {"t": "Benchmarking — Performance Evaluation & Quality Metrics", "b": "Performance benchmarks & metrics", "x": "benchmarking — performance evaluation & quality metrics comprehensive benchmarking system for evaluating retrieval quality, answer accuracy, and system performance. the benchmark suite provides quantitative metrics to track improvements and regressions. --- 1. overview the benchmarking system evaluates three dimensions: 1. retrieval quality — how well does the system find relevant documents? 2. component performance — how fast are individual components (chunking, embedding, reranking)? 3. end-to-end accuracy — how accurate are the final answers? benchmark suite structure --- 2. running benchmarks full retrieval benchmark (200 questions) what it does: - loads 200 legal questions with expected sources - runs each query through the retrieval pipeline - computes recall@k, mrr, precision@k, keyword match - generates failure analysis - produces \u0001 expected runtime: ~15-25 minutes (depends on gpu) subset benchmark (for quick iteration) runs only the first 20 questions (~2 minutes). benchmark with different configurations --- 3. benchmark metrics recall@k definition: fraction of queries where at least one expected source appears in the top-k results. example: - query: \"what is rescission?\" - expected: \u0001 - top-5 results: \u0001 - recall@5 = 1.0 (expected source found) interpretation: - recall@1 = 0.75 → 75% of queries have the right document as 1 result - recall@5 = 0.92 → 92% of queries have the right document in top-5 - recall@10 = 0.95 → 95% of queries have the right document in top-10 mean reciprocal rank (mrr) definition: average of \u0001 where \u0001 is the position of the first expected source. example: - query 1: expected source is 1 → 1/1 = 1.0 - query 2: expected source is 3 → 1/3 = 0.333 - query 3: expected source not in top-10 → 0.0 - mrr = (1.0 + 0.333 + 0.0) / 3 = 0.444 interpretation: - mrr = 1.0 → perfect (every query's top result is correct) - mrr = 0.5 → expected source typically at rank 2 - mrr = 0.25 → expected source typically at rank 4 precision@k definition: fraction of top-k results that are relevant (in expected sources). example: - expected: \u0001 - top-5: \u0001 - precision@5 = 2/5 = 0.4 interpretation: - precision@3 = 0.8 → 80% of top-3 results are relevant - precision@5 = 0.6 → 60% of top-5 results are relevant keyword match rate definition: fraction of expected keywords found in the text of the top result. example: - expected keywords: \u0001 - top result text: \"the contract may be rescinded...\" - found: 2/3 (\u0001, \u0001) → 0.667 interpretation: - kw match = 1.0 → top result contains all expected keywords - kw match = 0.5 → top result contains half the expected keywords - kw match = 0.0 → top result contains none (likely wrong document) --- 4. benchmark dataset the main benchmark uses 200 hand-curated legal questions with ground truth: dataset statistics category count description ---------- ------- ------------- contract law 60 contract formation, terms, remedies tort law 40 negligence, liability, damages property law 35 real property, leases, ownership criminal law 25 offenses, defenses, procedure civil procedure 20 pleadings, discovery, trials constitutional law 20 rights, powers, amendments difficulty count ------------ ------- easy 80 single concept, common terms medium 90 multi-concept, some jargon hard 30 complex, rare terms, edge cases get stats programmatically: --- 5. reading benchmark results after running \u0001, results are saved to \u0001: \u0001 (human-readable) \u0001 (machine-readable) \u0001 (per-question details) \u0001 (failure analysis) --- 6. failure analysis the benchmark automatically categorizes failures: failure type meaning possible fix -------------- --------- -------------- missing data expected source not in corpus add missing documents embedding problem query/doc embeddings too dissimilar fine-tune embedder on legal text ocr problem ocr quality poor for scanned docs improve ocr preprocessing reranker problem reranker demoted correct results tune reranker threshold keyword mismatch query uses different terminology add synonyms or query expansion error pipeline exception fix the bug diagnosing a failure opens an interactive session: --- 7. golden evaluation the golden eval is a smaller, curated set of high-confidence questions with verified answers — used to track answer quality (not just retrieval). running golden eval what it does: - loads 50 golden questions with verified correct answers - runs each through the full rag pipeline (retrieval + llm answer) - compares generated answer to expected answer (lexical + semantic similarity) - computes answer accuracy metrics golden eval metrics metric meaning -------- --------- exact match fraction of answers that exactly match expected f1 score token-level f1 (precision × recall) semantic similarity cosine similarity of answer embeddings citation accuracy fraction of citations that are correct abstain rate fraction of queries where system abstained --- 8. component benchmarks test individual components in isolation: chunking performance tests: - chunks per second (throughput) - chunk size distribution (are chunks near target 480 tokens?) - section respect (chunks never split headers) output: embedding performance tests: - embedding throughput (chunks/sec) - gpu vs cpu speed - batch size impact output: reranker performance tests: - reranking throughput (pairs/sec) - quality impact (how much does reranking improve recall@5?) - speed vs batch size trade-off output: retrieval accuracy tests: - dense vs sparse vs hybrid accuracy - top-k impact on recall - search mode comparison output: --- 9. continuous benchmarking tracking performance over time save metrics after each benchmark run: plot trends: regression detection set a minimum acceptable threshold: run in ci: --- 10. benchmark configuration all benchmark config is in \u0001: override for custom configs: --- 11. adding new benchmark questions edit \u0001: question guidelines good question: - specific, answerable from corpus - expected sources exist in \u0001 - keywords are distinctive (not generic stopwords) - difficulty matches query complexity bad question: - too broad (\"tell me about law\") - expected source not in corpus - keywords too generic (\"law\", \"legal\", \"document\") --- 12. benchmarking best practices before benchmarking 1. ✅ ingest corpus — ensure all documents are indexed 2. ✅ restart backend — cold start (models warm up on first query) 3. ✅ close other apps — free gpu memory 4. ✅ use consistent config — same \u0001, \u0001, etc. interpreting results good retrieval: - recall@5 ≥ 0.85 - mrr ≥ 0.70 - keyword match ≥ 0.65 good performance: - mean latency ≤ 5 seconds per query - success rate ≥ 80% red flags: - recall@10 < 0.70 → corpus missing key documents - mrr < 0.50 → retrieval returning irrelevant results - keyword match < 0.50 → wrong documents retrieved improving metrics metric low likely cause fix ------------ -------------- ----- recall@5 < 0.80 missing documents add more corpus docs mrr < 0.65 ranking poor enable/tune reranker precision@3 < 0.50 too many false positives raise \u0001 keyword match < 0.60 wrong doc types retrieved check chunking/ocr quality latency 10s gpu slow or cpu fallback check gpu manager, batch size --- 13. benchmark ci integration run benchmarks in ci on every pr: --- 14. cuad benchmark the cuad (contract understanding atticus dataset) benchmark tests contract understanding: what it tests: - clause extraction from contracts - contractual term identification - legal concept recognition metrics: - exact match accuracy - f1 score (token-level overlap) - clause recall (fraction of clauses found) see \u0001 for details. --- 15. troubleshooting benchmarks \"pipeline not initialized\" cause: chromadb not populated fix: benchmark very slow cause: running on cpu, not gpu fix: check gpu availability: set device explicitly: oom during benchmark cause: batch size too large fix: lower batch size: results don't match readme cause: different corpus or config fix: use the exact corpus + config from the readme: - corpus: 511 legal documents (18,836 chunks) - config: hybrid search, reranker enabled, top k=5 --- 16. benchmark visualization generate charts from results after running a benchmark, generate visual charts: generated charts (saved to \u0001): 1. recall comparison.png - bar chart comparing recall@1, r@5, r@10 2. category breakdown.png - per-category recall@5 horizontal bars 3. metrics overview.png - 4-panel dashboard with all key metrics 4. latency distribution.png - histogram of query latencies 5. failure analysis.png - pie chart of failure types 6. performance over time.png - line chart tracking metrics across runs installation visualization requires matplotlib: custom input specify a different metrics file: performance tracking workflow track improvements across development: example charts recall comparison: - bar chart: r@1, r@5, r@10 - quickly see retrieval quality at different k values category breakdown: - horizontal bars: recall@5 per category - identify which legal domains perform best/worst metrics overview: - 4-panel dashboard: recall, precision, success by difficulty, summary stats - one-page snapshot of entire benchmark latency distribution: - histogram: query latency distribution - mean/median lines for reference - identify slow outliers failure analysis: - pie chart: breakdown of failure types - prioritize what to fix next performance over time: - line chart: r@1, r@5, mrr across multiple runs - track improvement/regression trends exporting for reports charts are saved as high-dpi png (150 dpi) for inclusion in: - technical documentation - progress reports - stakeholder presentations - github readme programmatic usage --- 17. related documentation doc coverage ----- ---------- \u0001 unit tests, integration tests \u0001 retrieval pipeline implementation \u0001 gpu performance tuning \u0001 ingestion pipeline \u0001 performance profiling tools"}, "configuration": {"t": "Configuration Reference — Complete Environment Variables & Settings", "b": "Every environment variable & setting", "x": "configuration reference — complete environment variables & settings comprehensive configuration guide for lawyer assistant. every tunable parameter, environment variable, and configuration file documented in one place. --- 1. configuration hierarchy settings are read in order of precedence (highest wins): example: --- 2. model configuration embedding model (bge-m3) env var default type meaning --------- --------- ------ --------- \u0001 \u0001 path local model directory \u0001 \u0001 int embedding output dimension (bge-m3 fixed) \u0001 \u0001 int max tokens per text (512 is optimal for 480-token chunks) \u0001 \u0001 int cap batch size (gpu manager may tune down from free vram) \u0001 \u0001 bool fp16 faster on gpu, auto-disabled on cpu \u0001 \u0001 bool l2-normalize embeddings (required for cosine similarity) example: reranker model (bge-reranker-v2-m3) env var default type meaning --------- --------- ------ --------- \u0001 \u0001 path local model directory \u0001 \u0001 int max tokens per query+doc pair \u0001 \u0001 int cap batch size (gpu manager may tune down) \u0001 \u0001 bool fp16 faster on gpu, auto-disabled on cpu example: llm (ollama) env var default type meaning --------- --------- ------ --------- \u0001 \u0001 int gpu layers (\u0001 = cpu-only, \u0001 = use gpu fully) \u0001 \u0001 str how long llm stays loaded after request \u0001 \u0001 int cpu threads (0 = auto) \u0001 \u0001 int fallback vram estimate for 8b-class llm \u0001 \u0001 url ollama http api endpoint why \u0001 by default: - embeddings + reranking are latency-critical for search - llm generation is least time-critical - keeping llm on cpu frees entire vram budget for embeddings/reranking - set to \u0001 if you prefer faster generation over faster search example: device selection env var default type meaning --------- --------- ------ --------- \u0001 \u0001 str \u0001 \\ \u0001 \\ \u0001 \\ \u0001 auto resolution: 1. try cuda (nvidia gpu) 2. try mps (apple silicon) 3. fall back to cpu example: --- 3. gpu manager configuration see \u0001 for gpu manager design. env var default type meaning --------- --------- ------ --------- \u0001 \u0001 bool master switch (\u0001 = disabled, transparent no-op) \u0001 \u0001 int vram kept free for driver/display/other apps \u0001 \u0001 str \u0001 \\ \u0001 \\ \u0001 \u0001 \u0001 int (adaptive) min free vram to keep hot model resident \u0001 \u0001 int (adaptive) uses to become \"hot\" \u0001 \u0001 float (adaptive) sliding window for hotness tracking \u0001 \u0001 int (adaptive) max models resident simultaneously \u0001 \u0001 bool auto-tune batch sizes from free vram \u0001 \u0001 int vram reserve when auto-tuning batch \u0001 \u0001 bool allow \u0001 under vram pressure \u0001 — bool legacy: \u0001→eager, \u0001→persistent (still honored) residency modes: - \u0001 — demote always (idle models never occupy vram) - \u0001 — keep hot models resident, demote cold (best of both worlds) - \u0001 — never demote (all used models stay resident) example: --- 4. chunking configuration config field default type meaning -------------- --------- ------ --------- \u0001 \u0001 int target tokens per chunk \u0001 \u0001 int overlap tokens between adjacent chunks \u0001 \u0001 bool never split across section boundaries \u0001 \u0001 int discard chunks smaller than this \u0001 \u0001 str \u0001 \\ \u0001 no env var overrides — chunking config is code-only (edit \u0001). example (code override): --- 5. storage configuration chromadb env var default type meaning --------- --------- ------ --------- \u0001 \u0001 path chromadb persistent directory (workspace overrides this) other settings (code-only): - \u0001: \u0001 - \u0001: \u0001 - hnsw parameters: \u0001, \u0001, \u0001 workspace override: - when a workspace is active → \u0001 - no workspace → \u0001 paths all path defaults are in \u0001 → \u0001: path default env var ------ --------- --------- \u0001 \u0001 — \u0001 \u0001 — \u0001 \u0001 — \u0001 \u0001 — \u0001 \u0001 — workspace overrides: - \u0001 → \u0001 (uploads land directly in project folder) - \u0001 → \u0001 - \u0001 → \u0001 - \u0001 → \u0001 --- 6. search & retrieval configuration config field default type meaning -------------- --------- ------ --------- \u0001 \u0001 int initial retrieval from chromadb + bm25 \u0001 \u0001 int final result count after reranking \u0001 \u0001 float drop chunks below this reranker score \u0001 \u0001 bool enable reranking \u0001 \u0001 str \u0001 \\ \u0001 \\ \u0001 \u0001 \u0001 int rrf constant (higher = less weight to rank) \u0001 \u0001 str \u0001 \\ \u0001 \\ \u0001 no env var overrides — search config is: 1. code defaults (\u0001) 2. runtime pipeline config (\u0001) 3. per-request overrides (\u0001, \u0001) example (runtime config): example (per-request override): --- 7. ingestion configuration config field default type meaning -------------- --------- ------ --------- \u0001 \u0001 bool ocr for scanned pdfs \u0001 \u0001 bool extract table structure \u0001 \u0001 list ocr languages \u0001 \u0001 bool generate page images (improves ocr) \u0001 \u0001 int image dpi for ocr fallback \u0001 \u0001 int max pages per document (none = unlimited) no env var overrides — ingestion config is code-only. example (disable ocr for faster ingestion): --- 8. logging configuration env var default type meaning --------- --------- ------ --------- \u0001 \u0001 str \u0001 \\ \u0001 \\ \u0001 \\ \u0001 log files: - \u0001 — rotating file (10 mb, 5 backups) - console (stderr) — same level as file example: programmatic setup: --- 9. agent configuration env var default type meaning --------- --------- ------ --------- \u0001 \u0001 int max chars buffered as post-tool cot \u0001 \u0001 int max short segments counted as reasoning \u0001 \u0001 int max chars for a segment to be \"short\" what it controls: - untagged text after a tool result is treated as chain-of-thought (cot panel) - adaptive window: only the first n short segments count as reasoning - longer segments mean the answer body has begun → stop buffering cot example: --- 10. api provider configuration see \u0001 for cloud mode setup. configuration file file: \u0001 (git-ignored) environment variable overrides env var json key type meaning --------- ---------- ------ --------- \u0001 \u0001 bool master switch \u0001 \u0001 str \u0001 \\ \u0001 \\ \u0001 \\ \u0001 \\ \u0001 \u0001 \u0001 url main endpoint \u0001 \u0001 str main api key \u0001 \u0001 str llm model name \u0001 (override) url llm-specific endpoint \u0001 (override) str llm-specific key \u0001 \u0001 str embedding model name \u0001 (override) url embeddings-specific endpoint \u0001 (override) str embeddings-specific key \u0001 \u0001 int embedding dimension (0 = auto-detect) \u0001 \u0001 str \u0001 \\ \u0001 \\ \u0001 \\ \u0001 \\ \u0001 \u0001 (override) url rerank-specific endpoint \u0001 (override) str rerank-specific key \u0001 (override) str rerank model name \u0001 \u0001 float llm temperature \u0001 \u0001 int llm max tokens \u0001 \u0001 int api timeout (seconds) \u0001 \u0001 int anthropic extended thinking budget (tokens) precedence: env vars json file code defaults example: --- 11. workspace configuration active workspace set via api: or via frontend \"work in a folder\" button (native directory picker). workspace storage when a workspace is active: - raw files → \u0001 - chromadb → \u0001 - bm25 → \u0001 - processed → \u0001 - history → \u0001 runtime-only (resets on restart or \u0001). per-project gpu profile stored in \u0001 under \u0001: applied automatically when workspace is selected. --- 12. frontend configuration build-time configuration file: \u0001 (git-ignored) usage: runtime configuration (electron) file: \u0001 --- 13. launcher configuration file: \u0001 (written by launcher) see §10 api provider configuration. launcher-specific env vars: env var default meaning --------- --------- --------- \u0001 \u0001 path to api config file \u0001 (auto-detected) python interpreter for backend \u0001 \u0001 default ollama model to pull --- 14. configuration files reference file purpose git-ignored? ------ --------- -------------- \u0001 api provider settings ✅ yes \u0001 per-project chat history + gpu profile ❌ no (user data) \u0001 langgraph thread state (sqlite) ❌ no (user data) \u0001 compliance flags (sqlite) ❌ no (user data) \u0001 rotating log file ✅ yes \u0001 frontend build-time config ✅ yes --- 15. configuration validation check current config: returns: check gpu config: check api provider: --- 16. common configuration scenarios scenario 1: low-vram gpu (4gb) scenario 2: high-vram gpu (24gb+) scenario 3: cpu-only (no gpu) scenario 4: cloud api mode (no local models) scenario 5: debugging --- 17. configuration troubleshooting symptom likely cause fix --------- -------------- ----- \"config not loaded\" typo in env var name check \u0001 prefix api key rejected wrong key or expired check \u0001 gpu not used device set to cpu check \u0001 slow ingestion batch size too small increase \u0001 oom during search batch size too large decrease batch sizes logs not created logs dir missing \u0001 workspace not active no folder selected \u0001 --- 18. related documentation doc coverage ----- ---------- \u0001 gpu configuration deep dive \u0001 cloud mode configuration \u0001 workspace configuration \u0001 runtime pipeline config \u0001 config-related failures \u0001 dev environment config"}, "deployment": {"t": "Deployment — Building & Packaging for Production", "b": "Production deployment & hardening", "x": "deployment — building & packaging for production complete guide to building, packaging, and distributing lawyer assistant as a production desktop application for windows, macos, and linux. --- 1. build overview architecture key difference from dev: - frontend served from \u0001 (no vite dev server) - backend spawned by electron (no manual \u0001) - models downloaded on first run (not bundled, too large) --- 2. prerequisites tool version purpose ------ --------- --------- node.js 18+ frontend build, electron python 3.11+ backend runtime npm 9+ dependency management git any version control platform-specific: - windows: visual studio build tools (for node-gyp) - macos: xcode command line tools - linux: \u0001, \u0001, \u0001 --- 3. building the frontend step 1: install dependencies step 2: build what it does: 1. typescript compilation (\u0001) 2. vite production build (\u0001) 3. csp injection (production policy, no \u0001 scripts) 4. csp validation (\u0001) — fails on violation 5. output → \u0001 output structure verification expected output: --- 4. building electron step 1: install dependencies step 2: build main process what it does: 1. typescript compilation (\u0001) 2. path alias resolution (\u0001) 3. electron security check (\u0001) — fails on violation 4. output → \u0001 output structure verification expected output: --- 5. full build (frontend + electron) single command what it does: 1. \u0001 → \u0001 2. \u0001 → \u0001 both security checks run automatically — build fails on violations . --- 6. packaging packaging creates platform-specific installers using \u0001. package configuration file: \u0001 → \u0001 section package for all platforms creates installers for current platform only. platform-specific packaging windows: macos: linux: output --- 7. backend bundling what's bundled the backend is bundled as \u0001: installation on first run when the user launches the packaged app: 1. electron checks for \u0001 in app data directory 2. if missing: - runs \u0001 - runs \u0001 - downloads models (bge-m3, bge-reranker) on first query 3. starts backend: \u0001 user sees: - progress spinner during first-time setup - \"downloading models...\" on first query --- 8. code signing (optional) why sign? - windows: avoids \"unknown publisher\" warning - macos: required for gatekeeper (app store distribution) - linux: optional windows code signing prerequisites: - code signing certificate (\u0001 file) - password for certificate configuration: package with signing: macos code signing prerequisites: - apple developer account - developer id application certificate configuration: package with signing: --- 9. distribution option 1: github releases option 2: direct download host installers on your website: option 3: auto-updater use \u0001 for automatic updates: installation: configuration: requires: - signed builds - update server (github releases supported out-of-the-box) --- 10. platform-specific considerations windows installer type: nsis (default) or squirrel installation path: \u0001 auto-start: optional (add registry key) portable version: macos installer type: dmg (default) or pkg installation path: \u0001 gatekeeper: requires code signing for catalina+ notarization: required for macos 10.15+ (apple notary service) linux installer types: - appimage (portable, no installation) - snap (ubuntu software center) - deb (debian/ubuntu) - rpm (fedora/red hat) multiple targets: --- 11. build optimization reduce bundle size frontend: - tree-shaking (vite does this automatically) - remove unused dependencies - lazy load components backend: - exclude \u0001 from bundle: models: - don't bundle models (14gb too large) - download on first run (current approach) faster builds skip type checking (not recommended for production): parallel builds: cache electron binaries: --- 12. ci/cd for releases github actions workflow file: \u0001 trigger release github actions builds for all platforms and creates a release automatically. --- 13. testing the packaged app before distributing 1. smoke test: 2. verify: - app starts without errors - backend spawns successfully - models download on first query - chat works end-to-end - file upload works - workspace selection works 3. fresh user test: - test on a machine without dev tools installed - verify first-time setup (venv creation, model download) - check installer user experience --- 14. troubleshooting packaging issue cause fix ------- ------- ----- build fails security check failed run \u0001 large bundle models bundled exclude \u0001 from extraresources backend won't start python not found bundle python runtime (pyinstaller) missing dependencies \u0001 outdated update requirements icon not showing wrong icon format convert to .ico (win), .icns (mac) \"unknown publisher\" not code signed sign the executable macos gatekeeper blocks not notarized notarize the dmg --- 15. version management update version file: \u0001 (root, frontend, electron) update in all three: file: \u0001 automated version bump updates \u0001 and creates a git tag. --- 16. production environment variables set these in the packaged app environment: env var production value purpose --------- ------------------ --------- \u0001 \u0001 vite production mode \u0001 \u0001 less verbose logs \u0001 \u0001 enable gpu management \u0001 \u0001 hide dev warnings set in electron main process: --- 17. related documentation doc coverage ----- ---------- \u0001 security hardening, csp, sandboxing \u0001 environment variables, settings \u0001 common failures \u0001 dev environment, debugging \u0001 frontend build process \u0001 system overview"}, "architecture": {"t": "Architecture — Lawyer Assistant", "b": "System overview, ports, runtime modes", "x": "architecture — lawyer assistant a local-first legal research desktop app : an electron shell that launches a vite/react frontend and a python fastapi backend, with a rag (retrieval- augmented generation) engine over legal documents plus a compliance \"playbook scan\" feature. the app's ui, prompts and ocr are english, and retrieval itself is language-agnostic (the bge-m3 embedding model handles many languages, so non-english source text can still be searched). --- 1. big picture local-first: models (bge-m3 embeddings, bge-reranker-v2-m3) live in \u0001; the vector store lives in \u0001; llm inference is ollama running locally on \u0001. no cloud dependency. --- 2. ports & processes port owner purpose ------ ------- --------- 8765 \u0001 (uvicorn) fastapi \"lawyer assistant api\" — the frontend's only backend 5173 vite dev server serves the react app in development (\u0001) 11434 ollama local llm inference (chatollama / ollamallm) the electron main process kills anything listening on 8765/5173 at startup to avoid \u0001, then starts the python backend (auto-creating/using \u0001) and, in dev mode, the vite server. --- 3. runtime modes mode dev production ------ ----- ----------- electron load \u0001 (vite dev server) \u0001 backend spawned by electron (\u0001) bundled into \u0001 (\u0001 + requirements) run command \u0001 (root → runs electron, which boots backend + vite itself) \u0001 then \u0001 --- 4. the three ai flows 4a. chat / rag (search → answer) \u0001 → intent router → tools (\u0001, \u0001) → retrieval (dense+sparse+rerank) → llm answer with citations → sse events streamed to the ui. details: \u0001. 4b. compliance scan (document → flags) \u0001 (or \u0001 for new files) → chunk → classify clause type (embedding classifier) → check playbook rules → emit flags → persist to sqlite. details: \u0001 → \"playbook scan\". 4c. pipeline editor (visual config → backend reconfiguration) a reactflow canvas that visualizes the pipeline as nodes (intent, planner, dense, sparse, rerank, ingest, scan, answer) with removable/toggleable sub-nodes, localstorage persistence, and json export/import. since the \u0001 endpoint, the exported layout also reconfigures the python backend at runtime (rerank on/off, search mode + dense/sparse fusion weights, \u0001 overrides) via \u0001; \u0001 reads the active config on construction. details: \u0001, \u0001 §10. --- 5. key architectural decisions - single fastapi process owns both chat and scan; models warm up in a background thread at startup so the first query is fast. - sse everywhere for real-time ux: chat tokens, thinking, tool calls, node status, scan progress, and flags all stream as server-sent events. - two router generations : the default \u0001 mode uses the intent router (\u0001, plan-based, intent classification); the legacy agentic router (\u0001) is a langgraph react agent with human-in-the-loop approval and is used for \u0001/\u0001 modes (and by resume). - human-in-the-loop : langgraph \u0001 pauses before tool execution; the ui shows an approvalbar; \u0001 continues the thread. - persistence split : conversations persist in the browser (zustand \u0001); pipeline layout persists in \u0001; scan flags persist in backend sqlite; langgraph thread state persists in \u0001 (asyncsqlitesaver). - theme : light parchment mode is the default; the whole ui is driven by css variables (\u0001, \u0001, \u0001, …) with an \u0001 bridge that remaps tailwind utilities for contrast."}, "backend": {"t": "Backend — FastAPI server, agents, tools, retrieval & scan", "b": "FastAPI endpoints, routers, tools, retrieval, scan", "x": "backend — fastapi server, agents, tools, retrieval & scan this document covers everything python-side: the http api, the ai routers, tools, the retrieval pipeline, configuration, and the compliance scanner. --- 1. server basics - entry point: \u0001 → \u0001 - framework: fastapi, cors allows \u0001 and \u0001 - startup warmup (background thread, non-blocking): preloads the bge-m3 embedder, bge-reranker, the ollama llm (connection check), and the playbook classifier's reference embeddings — so the first user query is fast. - logging: \u0001 (\u0001), level from \u0001 (default info). --- 2. http api reference chat & files method path purpose -------- ------ --------- get \u0001 \u0001 — used by the app's boot screen post \u0001 non-streaming chat (collects events, returns final answer) post \u0001 sse streaming chat (primary path used by the ui) post \u0001 sse resume after human-in-the-loop approval post \u0001 save an attached file into the active project folder (a workspace must be selected first — there is no default \u0001 anymore), returns \u0001 post \u0001 apply a pipeline-layout json (see \u0001) — reconfigures retrieval at runtime (rerank on/off, search mode, top k, dense/sparse weights); optional query overrides \u0001, \u0001, \u0001, \u0001 get \u0001 current active runtime retrieval config delete \u0001 reset retrieval to file defaults workspace (user-picked folder) — required for chat/upload method path purpose -------- ------ --------- post \u0001 \u0001 — make a folder the active workspace ; uploads land directly in the folder, and the chromadb / bm25 / processed / history files all resolve under \u0001; returns \u0001 (supported docs discovered in the folder) get \u0001 current workspace (\u0001) delete \u0001 reset — no workspace active afterwards; chat/upload stay disabled until a new folder is picked post \u0001 ingest every supported document in the workspace folder into its chromadb index; returns \u0001 post \u0001 sse ingest with progress (\u0001, \u0001, \u0001, \u0001, \u0001); \u0001 re-indexes already-indexed files post \u0001 stop an in-flight streaming ingest cleanly at the next file boundary get \u0001 per-project saved state (\u0001) from \u0001 post \u0001 persist per-project state (chat history + working set + gpu profile) get \u0001 background folder-watcher status (watching?, last scan, what it indexed) a workspace is mandatory. chat (\u0001, \u0001, \u0001) and uploads error with \"no workspace selected. select a project folder first.\" when none is active — there is no default \u0001 folder fallback anymore. the folder is picked through a native electron directory dialog (\u0001 ipc) so the real absolute path reaches the backend. storage layout: with a workspace active, \u0001 returns the project folder itself (uploads land directly in the user's folder, visible as project documents), while processed temp files, the chromadb index, the bm25 index and \u0001 all live under \u0001 (\u0001, \u0001, \u0001, \u0001). the override is runtime-only (resets on restart / delete). \u0001 fields: \u0001, \u0001 (\u0001 default \u0001 \u0001), \u0001 (default 5), \u0001, \u0001 (default 0.3), \u0001, \u0001 (prior \u0001 turns for direct mode), \u0001 (raw selected passage from the native right-click menu — the pipeline retrieves the exact chunk the selection came from and cites it), and attachment fields \u0001 / \u0001. \u0001 behavior: - \u0001 → intent router (\u0001): classify intent, plan, then call tools; answer from documents. - \u0001 → no llm, just search; answer is a formatted list of results. - \u0001 → llm only, no retrieval. gpu manager method path purpose -------- ------ --------- get \u0001 device, free/total vram, per-model placement + hotness, resident ollama models get \u0001 the currently-active residency profile post \u0001 apply a profile now and persist it to the active project's \u0001 playbook scan method path purpose -------- ------ --------- get \u0001 list available playbooks (\u0001) post \u0001 non-streaming scan of an indexed document post \u0001 sse scan with progress (\u0001, \u0001, \u0001, \u0001, \u0001, \u0001, \u0001) post \u0001 sse save file → ingest (chunk/embed/store) → scan get \u0001 list flags, optional \u0001 / \u0001 filters get \u0001 single flag post \u0001 \u0001: \u0001 \u0001 \u0001 get \u0001 file names currently indexed in chromadb --- 3. chat flow (end-to-end) sse event types the frontend consumes (see \u0001): \u0001, \u0001, \u0001, \u0001, \u0001, \u0001, \u0001, \u0001, \u0001, \u0001, \u0001, \u0001, \u0001, \u0001, \u0001. (workspace ingest has its own event set: \u0001, \u0001, \u0001, \u0001, \u0001.) --- 4. agents, tools & modes 4a. intent router (\u0001) — default rag path a plan-based orchestration. key nodes (async generators that yield events): - \u0001 — chitchat / clarification / direct-answer intents - \u0001 — the main search path: plans, calls \u0001 (exact query, never rephrased), then \u0001, then writes the answer from the retrieved chunks. - \u0001 — \u0001 for a single attached file - \u0001 — batch scan of multiple attached files - \u0001 / \u0001 — entry points - uses \u0001 (a strict \"you must not answer directly; pick a tool\" prompt) and \u0001. - thread state checkpointer: \u0001 at \u0001 (falls back to \u0001). 4b. legacy agentic router (\u0001) a langgraph react agent (\u0001 + \u0001), used by \u0001/\u0001 modes and resume: - 3-node \u0001: \u0001 - human-in-the-loop: \u0001 calls \u0001 before tool execution; the graph pauses; the frontend shows an approvalbar; the user approves/rejects; \u0001 streams \u0001. on reject, a message tells the agent to answer without tools. - \u0001 routes: tool calls → approval; rejection message → agent; otherwise end. - system prompt rules: pass the user's query exactly as-is to \u0001 , never rephrase; cite file + page; be honest when documents don't contain the answer. 4c. tools (\u0001) tool signature what it does ------ ----------- -------------- \u0001 \u0001 runs the retrieval pipeline over chromadb (+bm25) and returns ranked chunks with file/page/section/score \u0001 \u0001 llm check for entity/topic match; returns \u0001 flags \u0001 \u0001 runs the compliance scanner over an indexed document, persists flags \u0001 \u0001 parse → chunk → embed → store into chromadb; makes the file searchable/scannable \u0001 / \u0001 build the langchain-compatible tool list. --- 5. retrieval pipeline (\u0001) all storage paths resolve through the active workspace (see §2 workspace): when a user-picked folder is active, \u0001 points at \u0001 and \u0001 at \u0001, so search and ingestion are isolated per project. \u0001 skips the workspace's own storage dirs by default. \u0001 → \u0001 (docling parse + ocr) → \u0001 (480-token chunks, \u0001) → embed → store → returns chunk count. also builds/updates the bm25 index. configuration (\u0001) all tunables are dataclasses, overridable with \u0001 env vars: - \u0001: \u0001 (1024-dim), \u0001; local dirs \u0001, \u0001; device \u0001 → cuda/mps/cpu. - \u0001: chunk size 480, overlap 0, respect sections, min 32 tokens. - \u0001: \u0001 (or \u0001 when a workspace is active), collection \u0001, cosine, hnsw. - \u0001: top k retrieval 50, top k final 5, min score 0.0, search mode \u0001, hybrid merge strategy \u0001 (default; \u0001 / \u0001 configurable), rrf k=60. - \u0001: docling ocr (en), page images, 150 dpi, table structure. --- 6. playbook scan (\u0001) a clause-level compliance checker : - playbooks: json files in \u0001 (e.g. \u0001); loaded by \u0001 (\u0001, \u0001). - persistence: flags stored in sqlite via \u0001 (\u0001, \u0001, \u0001); statuses \u0001 → \u0001 / \u0001 / \u0001. - classifier: \u0001 — embedding-based clause-type classification with \u0001 singleton and \u0001. --- 7. llm integration (\u0001) - \u0001 wrapper around ollama at \u0001. - the langgraph router binds \u0001. - rag prompts live in \u0001 (chat/rag prompts, extraction, no-context and error templates); intent prompts in \u0001. --- 8. non-streaming chat pipeline (\u0001) \u0001 → \u0001 — retrieval → rag prompt with context → llm generation → formatted response with citations. used by \u0001 for \u0001/\u0001 modes. --- 9. rag graph pipeline (\u0001) an alternative node-graph implementation with \u0001, \u0001, \u0001, \u0001, \u0001, \u0001; \u0001, \u0001, and streaming variants (\u0001, \u0001, \u0001)."}, "frontend": {"t": "Frontend — React UI, stores, streaming & theming", "b": "React layout, stores, streaming, theming", "x": "frontend — react ui, stores, streaming & theming the frontend is a vite + react 18 + typescript spa with tailwind css, zustand state management, reactflow for the pipeline editor, and react-markdown for rendering answers. it talks only to the fastapi backend at \u0001. --- 1. layout (\u0001) - boot gates the whole ui on \u0001 succeeding (bootloader shows loading/error with retry). - keyboard shortcuts: keys 1–5 select sources, arrows navigate, esc closes modals, cmd/ctrl+k toggles the command palette. --- 2. chat flow (\u0001) 1. ensure a conversation exists; append the user message; start streaming. 2. if \u0001 exist → \u0001 each (sequential, with status events), collect \u0001/\u0001; if the user typed nothing, the query becomes a default batch-scan prompt (\"scan these documents for compliance issues: …\"). 3. \u0001 — an async generator parsing sse lines. 4. event switch: - \u0001 — ignored - \u0001 — status line with icon + confidence - \u0001 → \u0001 - \u0001 → \u0001 (chain-of-thought) - \u0001 / \u0001 → streaming tool cards - \u0001 → plan result line (the routing directive, humanized) - \u0001 / \u0001 → execution log (pipeline nodes) - \u0001 → \u0001 - \u0001 → \u0001 (shows approvalbar) - \u0001 → clear approval, continue - \u0001 → \u0001 - \u0001 → \u0001 5. if the stream ends without \u0001 and no approval is pending, finalize anyway (interrupt fallback). \u0001 calls \u0001 and feeds the same event pipeline. --- 3. state management (\u0001) zustand store, persisted per project : the storage key is \u0001 plus a per-project hash suffix (\u0001, re-pointed via \u0001 when the workspace changes), and the same data is also saved server-side to the project's \u0001 (only \u0001 + \u0001 + starred messages are persisted; streaming state is ephemeral; \u0001 objects are rehydrated via \u0001). key state: - \u0001, \u0001, \u0001, \u0001 - streaming: \u0001, \u0001, \u0001, \u0001, \u0001, \u0001, \u0001 - hitl: \u0001, \u0001, \u0001, \u0001, \u0001, \u0001 - actions: create/select/delete conversation (auto-titles from the first user message), addmessage, all streaming append/update actions, \u0001 (computes \u0001: totalms, approvallatencyms, retrycount, nodetimings; stores thinking/statuses/ toolcalls on the message), \u0001/\u0001, \u0001. --- 4. api layer (\u0001) - \u0001 - \u0001, \u0001, \u0001 - \u0001 → formdata to \u0001 (requires an active workspace) - \u0001 / \u0001 — sse parsers yielding typed \u0001s - workspace: \u0001, \u0001, \u0001, \u0001 (sse with \u0001/\u0001/ \u0001/\u0001/\u0001), \u0001, \u0001, \u0001, \u0001 - gpu profile: \u0001, \u0001 - scan: \u0001, \u0001, \u0001, \u0001, \u0001, \u0001 --- 5. right-panel components sourcespanel (\u0001, 360px) - pipelinedashboard — metrics for recent messages (queries, avg time, approval latency, retry count). - pipelineexecution — the execution log of the current/last query (nodes, statuses, elapsed ms). - sourceitem list — each source shows file, page, section, score; click opens the sourceviewermodal . - workingsetpanel — pinned/selected sources. - memo export — \u0001 → copies a legal memorandum to the clipboard (\"memo\" button). workspacepanel shows files discovered from a selected folder (supported document extensions only, capped at 50) with per-file remove and clear-all. scanpanel (\u0001) - document + playbook selectors; \"start scan\" (50mb upload cap). - live progress bar (current/total, phase, %), streaming statuses. - result summary card (chunks, classified, flags, timing). - flags list with severity colors/icons and resolve/dismiss/escalate (optimistic update, revert on failure). - auto-scans an attached file once when opened with \u0001. chat components (\u0001) \u0001, \u0001, \u0001 (react-markdown + gfm), \u0001, \u0001, \u0001 (attachment chip list, drag- and-drop), \u0001, \u0001 (compliance flags on the answer), \u0001, \u0001, \u0001, \u0001, \u0001. --- 6. streaming ux - tokens append live into \u0001; the streaming message card shows statuses, chain-of-thought, tool calls with results, and inline citations. - node statuses (\u0001/\u0001) drive the execution log and the pipeline editor's live highlighting. - on \u0001, the message is finalized and stored with its sources, execution log, metrics, thinking, statuses, and tool calls. --- 7. theming (light & dark) - default is light parchment ; \u0001 toggles \u0001 on the root element (persisted). - all colors are css variables in \u0001 (\u0001, \u0001, \u0001, \u0001, \u0001/brass family, \u0001, \u0001, \u0001). - an \u0001 \"bridge\" remaps tailwind utility classes (\u0001, \u0001, severity colors, placeholder, hover variants) so components styled for dark mode stay readable on parchment. no opacity-variant color classes remain; white text only appears on accent/gradient/amber backgrounds. - fonts: newsreader (display/serif headings), plus jakarta sans (ui), jetbrains mono (labels). --- 8. tooling & scripts - \u0001: \u0001 (vite), \u0001 (tsc && vite build), \u0001, \u0001 (\u0001). - root \u0001: \u0001 runs the electron app (\u0001), which boots the backend and vite itself; \u0001/\u0001/\u0001 via electron-builder."}, "data-flow": {"t": "Data Flow", "b": "Document ingestion: parse → chunk → embed → index", "x": "data flow how data actually moves through the system — from raw files to retrieval results. --- 1. ingestion two ingestion paths exist: - app path (workspace ingest) — \u0001: docling parse → \u0001 ( 480-token chunks, overlap 0, sections never split , per \u0001) → embed → chromadb + bm25. this is what the ui uses when you attach files or run \"ingest all files\". - cli bulk path — \u0001 (legacy): simple char splitting (\u0001). used for one-time bulk imports only. the diagram below shows the cli path. chunking happens once , in memory. chunks are not saved to disk before embedding. everything is collected in a python list, then embedded in one batch, then saved together. --- 2. query / retrieval --- 3. key insight: what's saved where phase what where when ------- ------ ------- ------ chunking \u0001 python memory during phase 1 embedding \u0001 python memory during phase 2 backup \u0001 + \u0001 \u0001 after phase 2 chromadb vectors + text + metadata \u0001 phase 3 bm25 inverted index \u0001 phase 3.5 chunks are not saved to disk independently. they exist: 1. in memory during ingestion (phase 1-2) 2. as part of chromadb records (phase 3) 3. as part of the bm25 pickle (phase 3.5) 4. as a json backup (phase 2, optional) if ingestion crashes between phase 2 and phase 3, chunks are lost (the backup helps recover). --- 4. what doesn't exist yet - no persistent chunk cache between ingestion and embedding - no incremental ingestion (full rebuild required) - no chunk versioning or deduplication across runs - no pre-computed chunk embeddings cached separately from chromadb"}, "chatbot-flow": {"t": "What Really Happens When You Chat With This Chatbot", "b": "Message → intent → tools → rerank → LLM → answer", "x": "what really happens when you chat with this chatbot a complete, step-by-step walkthrough of every system that fires from the moment you type a message until you see the answer. --- system overview what's indexed (example corpus): 511 legal documents → 18,836 chunks in chromadb. your project's numbers depend on the folder you select — with a workspace active the index lives under \u0001. --- the complete flow: step by step step 0: server startup (happens once) when you run \u0001 in the \u0001 folder: step 1: you type a message what you see: you type \"hi\" or \"what is rescission in contract law?\" and press enter. what happens in the frontend (\u0001): step 2: backend receives the request file: \u0001 step 3: intent classification (0.3ms) file: \u0001 the system instantly classifies your message into one of two categories: greeting detection rules: - exact matches: \u0001, \u0001, \u0001, \u0001, \u0001, \u0001, \u0001, \u0001, \u0001, \u0001, \u0001, \u0001, \u0001 - regex patterns: \u0001 - everything else → planner path step 4a: greeting path (instant, no llm) if intent = greeting: total time: <100ms (string formatting only) --- step 4b: planner path (the main flow) file: \u0001 step 4b.1: planner node (llm call 1 — plan only) step 4b.2: directive parser the directive text is parsed to extract the tool to call and its arguments: step 5: tool execution — search documents file: \u0001 → \u0001 this is where the heavy lifting happens. the search has 6 sub-steps : --- step 5.1: query embedding (bge-m3) step 5.2: dense retrieval (chromadb) step 5.3: sparse retrieval (bm25) why both? dense catches semantic meaning (\"contract termination\" ≈ \"rescission\"). sparse catches exact keywords (\"rescission\" appears verbatim in a clause). neither alone is perfect. step 5.4: hybrid merge (combine and dedup) the default merge strategy is combine and dedup (union of dense + sparse results, deduplicated by chunk id); \u0001 (reciprocal rank fusion) and \u0001 are configurable alternatives. step 5.5: reranking (bge-reranker-v2-m3) step 5.6: final selection step 6: answer generation (llm call 2) file: \u0001 — answer node step 7: response streaming to frontend file: \u0001 — sse event generator step 8: frontend renders the response file: \u0001 --- complete timeline for a legal query breakdown: phase time % of total ------- ------ ------------ intent classification <1ms 0% planner (llm 1) ~2.5s 15% query embedding ~0.1s 1% dense retrieval ~0.5s 3% sparse retrieval ~0.3s 2% merge <0.01s 0% reranking ~8s 47% answer generation (llm 2) ~5s 30% network overhead ~0.5s 3% the reranker is the bottleneck — nearly half the total time. --- the scan path (when you attach a file) when you attach a document, the intent classifier routes to the scan pipeline instead: --- models used model purpose size device latency ------- --------- ------ -------- --------- lfm2.5:8b llm (planner + answer) 8b params ollama 2-15s per call bge-m3 query/chunk embedding 568m params cuda fp16 0.1s cached, 4s cold bge-reranker-v2-m3 cross-encoder reranking 568m params cuda fp16 8s for 100 pairs bm25 keyword search n/a (index) cpu 0.3s --- data storage store contents location ------- ---------- ---------- chromadb chunk vectors + metadata \u0001 (workspace active) or \u0001 bm25 index keyword search index (pickle) \u0001 (workspace active) or \u0001 sqlite conversation memory (checkpointer) \u0001 (project-independent) sqlite scan compliance flags \u0001 (project-independent) filesystem source documents the active project folder itself (workspace required — no default \u0001 anymore) filesystem ml model weights \u0001 --- key design decisions 1. two llm calls per query — planner decides what to search, answer writes how to respond. separation of concerns. 2. hybrid search (dense + sparse) — dense catches meaning, sparse catches exact terms. neither alone is sufficient for legal text. 3. cross-encoder reranking — much more accurate than embedding similarity alone, but 50x slower. worth it for accuracy. 4. anti-hallucination prompt — the answer node is told to never invent facts, only use document text with citations. 5. source text storage — full original paragraphs stored alongside chunks so the sourceviewermodal shows complete context. 6. greeting bypass — \"hi\" gets an instant response without touching the llm or retrieval pipeline. 7. hitl (human-in-the-loop) — the agent can interrupt before taking actions, waiting for user approval via the resume endpoint."}, "pipeline-editor": {"t": "Pipeline Editor — visual pipeline configuration", "b": "Visual canvas, sub-nodes, toggles, bypass", "x": "pipeline editor — visual pipeline configuration the pipeline editor is a reactflow canvas (right panel of the app) that visualizes the retrieval/scan pipeline as nodes and edges, lets users strip or restore sub-steps, persists the layout, and supports json export/import. scope note: this is both a visualization surface and a configuration surface . since the \u0001 endpoint was added, the exported layout reconfigures the python backend at runtime — rerank on/off from the \u0001 node, search mode + dense/sparse fusion weights from the retrieval nodes, and \u0001 overrides. config is runtime-only (resets on restart). see \u0001 §10 for the derivation rules. --- 1. where it lives - \u0001 - \u0001 — canvas, state, persistence, export/import, live status highlighting - \u0001 — node components + the shared \u0001 helper - \u0001 — \u0001 / \u0001 and the \u0001 edge type - \u0001 — draggable node types, sub-node toggle switches, legend, reset/export/import buttons - \u0001 — localstorage persistence + json serialize/parse - \u0001 — animated \"particle\" data-flow edges - opened via the pipeline button in the sources panel header; toggled from the sidebar's workspace button. it renders in a 560px right panel. --- 2. nodes node type id role ------ --------- ------ intent classifier \u0001 classifies greeting vs legal query vs scan chitchat \u0001 fast greeting reply, no retrieval planner \u0001 writes search plan, dispatches to dense + sparse dense retrieval \u0001 semantic search over embeddings (chromadb) sparse retrieval \u0001 keyword / bm25 search reranker \u0001 cross-encoder re-scores candidates ingest files \u0001 parse attached files → chunk → embed → store compliance scan \u0001 playbook scan + flags answer \u0001 writes final answer from retrieved documents only custom tool \u0001 generic drag-and-drop node default flow: \u0001 and \u0001. --- 3. removing a node — auto-bypass every node has a ✕ button. deleting a middle sub-node bypasses it: incoming edges are rewired straight to the outgoing targets. example: removing the reranker creates \u0001 and \u0001 edges, so the flow stays connected. - implemented by \u0001 (pure helper in \u0001): for each incoming × outgoing pair it builds a bypass edge. - bypass edges are tagged with ids starting \u0001 and labeled \u0001 — this tag is what lets re-enabling find and remove them. - the ✕ button (\u0001) and the palette toggle share this helper, so both removal paths behave identically. --- 4. re-enabling a removed sub-node (palette toggles) the nodepalette shows the five sub-node types (dense, sparse, rerank, ingest, scan) with an on/off toggle switch (\u0001, brass styling, icon dims to 45% when off): - off → remove: same auto-bypass as the ✕ button. - on → re-enable: re-adds the node at its default position and re-wires its original edges (\u0001), while removing the \u0001 edges that stood in for it. - smart restoration: edges whose counterpart node is also absent are skipped, and existing edge ids are never duplicated. implementation: \u0001 → \u0001 + \u0001 memo; \u0001 maps palette type → canonical node id. --- 5. persistence (localstorage) \u0001: - key: \u0001. - save: 300ms-debounced on any node/edge change + a \u0001 flush so the last edit survives reload. if the layout equals the shipped defaults, it clears storage instead of saving (so future default updates are never shadowed by a stale copy). - load: lazy-initialized on mount; falls back to \u0001 of defaults when nothing is saved. - sanitization: strips runtime-only fields (\u0001, \u0001, \u0001). --- 6. validation on load/import \u0001 is shared by localstorage loads and imported files: - rejects non-objects / missing \u0001/\u0001 arrays. - drops nodes with unknown types (e.g. stale \u0001/\u0001). - dedupes node ids and edge ids (first occurrence kept). - drops edges whose source/target nodes don't exist; drops id-less edges. - coerces unknown edge types to \u0001. - accepts a structurally-valid layout with zero nodes — a deliberately emptied canvas round-trips and survives reload. --- 7. export / import (json) - export (↓): downloads \u0001 — a pretty-printed \u0001 envelope (\u0001), sanitized. - full json schema: see \u0001 — the field-by-field spec (node types, edge rules, named handles, bypass-edge pattern) written so an ai model can author a valid layout file from scratch. - import (↑): hidden file input (\u0001); reads the file, parses + validates (\u0001 — accepts the versioned envelope or a bare \u0001), applies it, and remounts the canvas so the view re-fits. - reset (↺): clears storage, restores defaults, remounts the canvas. - transient feedback in the palette header: \"layout imported ✓\" / \"invalid layout file ✗\" / \"layout exported ✓\" (2.5s, timer cleaned up on unmount). --- 8. live status highlighting while a query streams, the canvas highlights the active node and dims/brightens edges: - \u0001 is derived from the last streaming status keyword (\u0001/\u0001 → ingest; \u0001/\u0001/\u0001 → scan; \u0001 → rerank; \u0001/\u0001 → dense; \u0001/\u0001 → sparse; \u0001/\u0001 → rerank; \u0001 → planner; \u0001 → answer; …). - nodes get \u0001 / \u0001 / \u0001 / \u0001 status classes; edges animate with particle flows and show dynamic payload previews (query, candidates, results, flags) during streaming. --- 9. styling all pipeline css is in \u0001 under the \"pipeline editor\" section and is theme-variable based (works in light parchment and dark mode): \u0001 + status variants, \u0001, \u0001, \u0001, \u0001, \u0001, \u0001 (reactflow overrides incl. minimap and controls), and the particle-edge animations."}, "pipeline-json": {"t": "Pipeline Layout JSON — spec for AI models & hand-editors", "b": "Exact JSON schema for pipeline layouts", "x": "pipeline layout json — spec for ai models & hand-editors this is the authoritative, machine-actionable spec for the pipeline-editor layout json. it is written so an ai model (or a human) can produce a valid file from scratch that the editor will import without errors. - ground truth code: \u0001 (serialize / parse / validate), \u0001 (default nodes & edges), \u0001 (node types + bypass edges). - feature overview: \u0001. - backend wiring: since the \u0001 endpoint was added, this json now also reconfigures retrieval at runtime (reranking, search mode, top k, dense/sparse weights). see \u0001. --- 1. file envelope (two accepted shapes) the import function \u0001 accepts either : if you author a file, prefer shape a with \u0001 (matching \u0001). \u0001 is informational — the parser does not check it. --- 2. node object schema rules enforced by \u0001: - \u0001 must be one of the 10 known types (§3). unknown/stale types are dropped silently (e.g. the pre-split \u0001 / \u0001). - duplicate node \u0001s: only the first occurrence is kept. - runtime-only fields (\u0001, \u0001, \u0001) are tolerated on import and overridden at render (the editor re-derives them); they are stripped on export. you may include them or omit them. --- 3. valid node types (known types) type canonical default id default position meaning ------ --------------------- ------------------ --------- \u0001 \u0001 {450, 0} classifies greeting vs legal query \u0001 \u0001 {100, 220} greeting reply, no retrieval \u0001 \u0001 {450, 220} writes search plan, dispatches to dense + sparse \u0001 \u0001 {280, 440} semantic search (chromadb) \u0001 \u0001 {620, 440} keyword search (bm25) \u0001 \u0001 {450, 660} cross-encoder re-scoring \u0001 \u0001 {100, 440} parse attached files → chunks → embed → store \u0001 \u0001 {100, 660} playbook compliance scan + flags \u0001 \u0001 {450, 880} final answer from retrieved docs \u0001 (any) (any) generic custom node (drag-and-drop) the five toggleable sub-nodes (palette switches) are: \u0001, \u0001, \u0001, \u0001, \u0001 — mapped to canonical ids \u0001, \u0001, \u0001, \u0001, \u0001. keep those ids if you want the toggle switches to control the node. --- 4. edge object schema rules enforced by \u0001: - edge is dropped if \u0001 or \u0001 does not reference an existing node id (that survived validation). - edge is dropped if \u0001 is missing or a duplicate (first occurrence kept). - \u0001 other than \u0001/\u0001 is coerced to \u0001. --- 5. named handles (important for the intent node) only the intent node exposes named source handles. if an edge leaves \u0001, set \u0001 to one of: sourcehandle meaning used by default edge -------------- --------- ---------------------- \u0001 greeting route \u0001 \u0001 legal-query route \u0001 \u0001 attachment route \u0001 all other nodes use a single unnamed source/target handle — omit \u0001/\u0001 for them. --- 6. bypass edges (what a removed sub-node looks like) when a middle sub-node is removed, the editor rewires incoming edges straight to outgoing targets. those rewires are tagged so re-enabling can find them: - id pattern: \u0001 (e.g. \u0001) - \u0001, \u0001, \u0001 - \u0001/\u0001 are the surviving nodes; \u0001/\u0001 are carried over from the incoming edge. example — the reranker removed : --- 7. a complete valid example (dense + sparse → answer, no rerank, no scan) this file is guaranteed to import cleanly — it references only known node types, unique ids, and edges whose endpoints exist: note: \u0001 ids are arbitrary here — the editor only matches them by the \u0001 prefix when re-enabling the reranker. using the real pattern keeps re-enable behavior correct. --- 8. generation checklist (ai must follow all of these) 1. envelope: \u0001. 2. every node: has string \u0001 (unique), valid \u0001 from §3, and a \u0001 object with numeric \u0001/\u0001. recommended: \u0001. 3. every edge: has string \u0001 (unique), \u0001 and \u0001 that both exist in the node list , and \u0001. 4. no dangling edges: an edge to a node you deleted is invalid — it will be silently dropped. 5. no unknown types: using \u0001, \u0001, or any other non-§3 type gets the node silently dropped. this is the most common ai mistake. 6. named handles only from intent (§5); omit them elsewhere. 7. zero nodes is legal (a deliberately emptied canvas round-trips), but a file with edges and no valid nodes is pointless — edges get dropped. 8. keep ids stable and semantic (\u0001, \u0001, \u0001, \u0001, \u0001, \u0001, \u0001, \u0001, \u0001) so palette toggles and re-enabling work. 9. extra fields are tolerated: the parser ignores unknown properties. --- 9. round-trip guarantee export writes \u0001 with runtime fields stripped (\u0001, \u0001, \u0001). import accepts that exact output back — export → import is identity-preserving. if you want to edit an exported file: change positions, delete nodes and every edge touching them , and keep all other fields as-is. --- 10. backend runtime configuration (\u0001) the backend validates and consumes exactly the same json this spec covers. the editor no longer needs to be visualization-only: sending the layout to the backend reconfigures retrieval for every subsequent query. endpoint - \u0001 — body is the layout json (shape a or b from §1). applies it at runtime. - \u0001 — returns the currently active config. - \u0001 — restore file defaults. optional query overrides on post (win over the node-derived values): \u0001 (1–100), \u0001 (1–500), \u0001, \u0001 (0–10). derivation rules (from which sub-nodes are present) sub-node state skip rerank search mode weights ---------------- ------------- ------------- --------- \u0001 present \u0001 (rerank on) — — \u0001 absent \u0001 (rerank off) — — \u0001 + \u0001 — \u0001 dense w / sparse w (default 1/1) only \u0001 — \u0001 sparse w forced to 0 only \u0001 — \u0001 dense w forced to 0 neither retrieval node — file default — precedence: file defaults < layout config < explicit per-request params (\u0001, \u0001 in the chat request). one exception: a layout with an explicit \u0001 override wins over the chat ui's fixed default of 5 (the frontend sends \u0001 unconditionally). behavior notes - validation reuses the same rules as the editor: unknown node types, dangling edges, and duplicate ids are dropped silently (§2–§4), so a file that imports cleanly into the editor also applies cleanly here. - the response returns the derived \u0001 plus a \u0001 summary (\u0001/\u0001, \u0001/\u0001, \u0001, \u0001). - \u0001, \u0001, \u0001, and weights are read by \u0001 on construction (\u0001), so every path — chat, tools, scan-upload ingestion queries — honors the layout automatically. - weights apply to the rrf fusion (\u0001) and, when non-default, to the combine-and-dedup ordering for the no-rerank path. - config is runtime-only : it resets on server restart unless re-applied. persistence lives in the editor's \u0001 (\u0001) and the exported json file. - ⚠️ empty layout caveat: applying \u0001 (a deliberately emptied canvas) derives \u0001 — reranking is disabled globally until another layout or \u0001 restores it. use \u0001 (or the editor's reset button, which is visual only) if you want retrieval untouched."}, "pipeline-roadmap": {"t": "Pipeline Loop — Improvements & Roadmap", "b": "Planned improvements to the RAG pipeline", "x": "pipeline loop — improvements & roadmap what the node-based pipeline system could benefit from, organized by priority. --- current state ✅ architecture components component file status ----------- ------ -------- core engine \u0001 ✅ done rag graph pipeline \u0001 ✅ done backend api wiring \u0001 ✅ done per-query visualization \u0001 ✅ done performance dashboard \u0001 ✅ done log storage per conversation \u0001 ✅ done note: effort estimates below are rough approximations for a single developer. actual time depends on testing, code review, and integration complexity. --- 🔴 high priority 1. query expansion node why: expands queries with synonyms and related terms before embedding — directly improves retrieval recall. implementation: - new node: \u0001 in \u0001 - uses llm or synonym dictionary to generate 2–3 query variants - runs retrieval for each variant, merges results via rrf - insert before retrieval: \u0001 estimated effort: 1–2 hours graph change: --- 2. streaming pipeline progress status: implemented. nodes execute in real-time instead of waiting for the entire pipeline to complete. implementation (as shipped): - backend: server-sent events (sse) on \u0001 - each node emits progress events: \u0001, \u0001 - frontend: a \u0001-based sse line parser (in \u0001) updates \u0001 in real-time - nodes pulse/highlight when active --- 🟡 medium priority 3. langgraph stategraph integration why: lets agents use the pipeline as langgraph tools for agentic workflows. implementation: - wrap \u0001 output as a langgraph \u0001 - each \u0001 maps to a langgraph node - conditional edges map to langgraph conditional routing - expose via \u0001 for agent use estimated effort: 2–3 hours --- 4. pipeline caching why: cache retrieval results for repeated/similar queries — saves embedding + search time. implementation: - add \u0001 that checks a query cache before retrieval - cache key: sha256 of normalized query text - cache storage: sqlite or in-memory lru with ttl - cache invalidation: on ingestion or manual \u0001 - hit/miss stats in the dashboard estimated effort: 2–3 hours --- 5. custom node composition why: let users define custom pipeline workflows via config (yaml/json). implementation: - define a pipeline schema: - \u0001 parses and builds the graph - store pipeline configs in \u0001 estimated effort: 3–4 hours --- 🟢 nice to have 6. pipeline debug endpoint why: full execution trace for debugging pipeline behavior. implementation: - new endpoint: \u0001 - returns full context dump: every node's input/output, timing, errors - frontend: \"debug\" button in the pipeline section opens a modal with the trace estimated effort: 1–2 hours --- 7. node timeout handling why: kill stuck nodes (e.g., llm hanging) after configurable timeout. implementation: - add \u0001 to \u0001 (already exists but unused) - \u0001 wraps \u0001 in a \u0001 or \u0001 - on timeout: log error, mark node as failure, continue pipeline estimated effort: 1–2 hours --- 8. a/b testing support why: compare different pipeline configurations side-by-side. implementation: - run the same query through two pipeline configs - compare: timing, answer quality, source diversity - dashboard shows side-by-side comparison - store comparison results for later analysis estimated effort: 2–3 hours --- 9. pipeline metrics export why: production monitoring with prometheus/grafana. implementation: - expose metrics: \u0001, \u0001, \u0001 - use \u0001 library - add \u0001 endpoint to fastapi estimated effort: 2–3 hours --- 10. pipeline versioning why: track pipeline configuration changes over time. implementation: - hash pipeline config (node types, edges, parameters) - store version alongside execution logs - dashboard shows which pipeline version produced each result - rollback support estimated effort: 2–3 hours --- effort summary priority feature effort ---------- --------- -------- 🔴 query expansion node 1–2h 🔴 streaming pipeline progress 3–4h 🟡 langgraph integration 2–3h 🟡 pipeline caching 2–3h 🟡 custom node composition 3–4h 🟢 debug endpoint 1–2h 🟢 node timeout handling 1–2h 🟢 a/b testing 2–3h 🟢 metrics export 2–3h 🟢 pipeline versioning 2–3h total estimated effort: ~20–28 hours --- recommended implementation order 1. query expansion node — highest impact on retrieval quality 2. node timeout handling — quick win, prevents hung pipelines 3. debug endpoint — makes development/debugging much easier 4. pipeline caching — performance improvement for repeated queries 5. streaming progress — best ux improvement 6. langgraph integration — enables agentic workflows 7. custom node composition — power user feature 8. metrics export — production readiness 9. a/b testing — research/optimization tool 10. pipeline versioning — long-term maintainability"}, "gpu-management": {"t": "Smart GPU Management — how the app shares a small GPU", "b": "VRAM sharing, RAM-first residency, auto-tuning", "x": "smart gpu management — how the app shares a small gpu this document explains how the lawyer assistant manages its gpu . it is written for humans first — if you are just here to understand what the app does with your graphics card, read §1 and §2 . if you want to tune it, read §5 . if you want to wire it into new code, read §6 . --- 1. the problem this solves this app runs three heavy ai models on one machine: model what it does when it runs size (fp16) ------- ------------- -------------- ------------- bge-m3 embedder turns text into vectors ingestion + every search query ~1.1 gb bge-reranker scores retrieved chunks every search ~2.2 gb (fp32) llm (ollama) writes the final answer every chat answer 5 gb class a typical laptop has an 8 gb gpu (rtx 4060 class) — often less, sometimes shared with the display. you cannot fit all three models on the gpu at once , and even two at once leaves no room for the activations (the temporary memory a batch of text needs while being processed). the naïve approach — just load everything onto the gpu and hope — ends in \u0001 at the worst moment. the app's answer is a smart gpu manager (\u0001) that acts like a tiny operating system for vram: it decides which model sits on the gpu at any moment , evicts the right one when space runs out, and even shrinks the batch size so a burst of work never exceeds the budget. --- 2. the core idea in one picture models live in ram. they visit the gpu only when they work. when the work is done, they come back to ram. re-visiting is fast — a pcie copy (~0.1–0.3 s), not a slow reload from disk. this is called ram-first residency : loading a model into host ram is cheap and happens once; moving it to vram and back per phase is a fast copy that leaves the model resident in ram so the next promotion is near-instant. --- 3. what actually happens during a chat query walk through one rag search and watch the gpu change hands: 1. embed the query → the manager promotes the embedder to vram, runs \u0001, then (usually) demotes it back to ram. 2. retrieve + rerank → the manager promotes the reranker , runs \u0001, demotes it. 3. llm generates the answer → the llm is not wrapped in promote/demote: it runs on cpu by default (\u0001) so it never steals vram from the latency-critical embedding/reranking phases. so during a single answer, vram is handed from the embedder to the reranker and then left alone while the llm writes text. the gpu is never idle and never overcommitted. priority — who wins when space is tight each model has a priority (lower number = higher priority): when a model needs vram and there isn't enough, the manager evicts idle lower-priority models first. it will never evict a higher-priority model to make room for a lower one — the embedder keeps its spot even if the reranker really wants it. among equal priorities, it evicts cold models before hot ones , then the least-recently-used. the one exception: the llm ollama owns its own weights, so the manager can't just \"demote\" it. instead, when vram is truly tight, the manager calls \u0001 on gpu-resident llms to free their vram. it uses the cli when available, and falls back to the ollama http api (\u0001) when the cli isn't on path — which is common on windows desktop installs, exactly the low-end target this manager is built for. this is off by default in the sense that it only triggers under pressure (\u0001). --- 4. three residency modes — and \"hot\" models after a phase ends, should the model stay on the gpu or go back to ram? that's the residency mode (\u0001), and it's the main tuning knob: mode behavior best for ------ ---------- ---------- \u0001 demote after every phase. gpu is almost always empty of models; every phase pays the promote cost. tiny gpus (4 gb), heavy multitasking \u0001 (default) demote cold models; keep hot models resident when there's vram headroom. most machines — best of both worlds \u0001 never demote after use; every used model stays resident. capable gpus, repetitive workloads what does \"hot\" mean? in \u0001 mode, a model becomes hot when it is used \u0001 (default 2) times within \u0001 (default 120 s) — a sliding window, so old uses expire. a model that keeps getting used (you're searching repeatedly) stays on the gpu and repeat queries skip the promote cost entirely . a model used once ages out and frees vram. three guards stop \"hot\" from becoming \"greedy\": 1. pressure always wins — if free vram drops below \u0001 (1 gb), even hot models are demoted. 2. resident cap — at most \u0001 (2) models stay resident at once; the least valuable idle resident is evicted to make room. 3. never evict higher priority — the cap cannot force out the embedder to keep the reranker. --- 5. auto-tuned batch sizes (the oom safety net) even with one model on the gpu, a huge batch can exhaust vram during inference. so the manager also auto-tunes the batch size at runtime: every \u0001 / \u0001 call asks the manager for a safe batch size. free vram is measured live via \u0001, so if the display or another app is eating vram, the batch shrinks — and grows back when space returns. the configured batch size (\u0001, \u0001) is now just the cap . per-sample estimates (tunable in \u0001): model mb per sample basis ------- -------------- ------- embedder 24 mb 512-token chunk, fp16 activations reranker 48 mb cross-encoder processes query+doc pairs (~2× the embedder) if the model couldn't be promoted at all (vram too tight) it runs on cpu, where the vram budget is meaningless — the manager keeps the configured batch instead of collapsing to batch 1, so low-vram machines that fell back to cpu aren't slowed down twice. --- 6. where the code lives concern file --------- ------ the manager itself (promote/demote/schedule/evict/tune) \u0001 all knobs, defaults, env vars, per-project profiles \u0001 embedder registers + uses \u0001 \u0001 reranker registers + uses \u0001 \u0001 llm registration (so status shows it / budget accounts for it) \u0001 http endpoints (\u0001) \u0001 tests (37 pure-logic tests, no gpu needed) \u0001 the public api for other code --- 7. per-project gpu profiles every workspace project can carry its own gpu residency profile (mode, hot window, max resident, …), stored inside \u0001 under the \u0001 key. switching folders reloads that project's profile automatically ; a project with no saved profile resets to defaults — settings never leak between projects. the gpu residency card in the ui's workspace panel is the friendly way to edit it. --- 8. configuration reference (all env vars) env var default meaning --------- --------- --------- \u0001 \u0001 master switch (\u0001 = transparent no-op, models stay in ram) \u0001 \u0001 vram kept free for driver/display/other apps \u0001 \u0001 \u0001 \\ \u0001 \\ \u0001 \u0001 — legacy: \u0001→eager, \u0001→persistent (still honored) \u0001 \u0001 adaptive: min free vram to keep a hot model resident \u0001 \u0001 uses inside the window to become \"hot\" \u0001 \u0001 hotness sliding window \u0001 \u0001 max models kept resident simultaneously \u0001 \u0001 auto-tune batch sizes from free vram (\u0001 = fixed batch) \u0001 \u0001 reserve beyond the model when auto-tuning batch \u0001 \u0001 allow \u0001 under vram pressure \u0001 \u0001 fallback footprint for an 8b-class llm \u0001 \u0001 \u0001 = llm on cpu (frees vram); \u0001 = let ollama use gpu \u0001 \u0001 ollama http api (used when cli not on path) http endpoints endpoint returns ---------- --------- \u0001 device, free/total vram, per-model placement + hotness, resident ollama models \u0001 the currently-active residency profile \u0001 apply a profile now and persist it to the active project --- 9. how to watch it work watch the backend log — the manager logs its decisions: or query the live snapshot: which reports each model's \u0001 (\u0001/\u0001), \u0001, \u0001, and \u0001 — everything the manager is doing right now. --- 10. design rules (so future changes don't break the balance) 1. ram-first, always. never keep an idle model on the gpu when a lower priority model needs the space. 2. never evict a higher-priority model for a lower one. 3. pressure beats preference. \u0001/\u0001/\u0001 all yield to insufficient vram. 4. slow operations never hold the lock. \u0001 (up to 30 s) runs outside both the manager lock and the fastapi event loop, so a concurrent chat phase is never blocked. 5. the refcount is set before promotion , so a concurrently-acquired slot can never be evicted mid-promotion. 6. the configured batch size is a cap, never a floor. auto-tuning only ever shrinks it."}, "api-provider": {"t": "API Provider Mode — run Lawyer Assistant on remote AI", "b": "Cloud mode, providers, env vars, rerank modes", "x": "api provider mode — run lawyer assistant on remote ai an optional mode where every model-backed step (llm generation, embeddings, reranking) runs through cloud/remote apis instead of local models. docling document parsing, bm25 sparse indexing and the chromadb vector index stay local by design — only model inference is delegated. local mode is 100% unaffected: unless a config file explicitly enables api mode, the app behaves exactly as before (ollama llm + bge-m3 + bge-reranker). --- 1. how it manages your models step local mode (default) api mode ------ ---------------------- ---------- document parsing (docling) local local (unchanged) embeddings (bge-m3) local gpu/cpu remote embeddings api vector index (chromadb) local local (unchanged) sparse index (bm25) local local (unchanged) reranking (bge-reranker) local gpu/cpu remote rerank api (or embedding-based) llm (ollama) local remote chat-completions api playbook scan classifier local bge-m3 remote embeddings gpu manager manages local vram no-op (nothing local to manage) the chromadb and bm25 indexes still live inside your project folder's \u0001 directory, so switching projects works exactly the same. 2. enabling api mode copy \u0001 to \u0001, set \u0001, fill in your key/model, and restart the backend: \u0001 is git-ignored — the api key never gets committed. you can also point at a different file with the \u0001 env var. \u0001 enables anthropic extended thinking (see the anthropic section below). \u0001 = off, the default. environment-variable overrides every field can be overridden by an env var (these win over the json file): env var meaning --------- --------- \u0001 \u0001/\u0001 to enable \u0001 \u0001 \\ \u0001 \\ \u0001 \\ \u0001 \\ \u0001 \u0001 / \u0001 main endpoint + key \u0001 / \u0001 / \u0001 llm override \u0001 / \u0001 / \u0001 embeddings override \u0001 embedding dimension (0 = auto-detect) \u0001 \u0001 \\ \u0001 \\ \u0001 \\ \u0001 \\ \u0001 \u0001 / \u0001 / \u0001 rerank endpoint \u0001 / \u0001 / \u0001 generation knobs \u0001 anthropic extended-thinking budget in tokens (\u0001 = off) 3. providers \u0001 / \u0001 — any openai-compatible api: openai, groq, deepseek, mistral, openrouter, together, jina, moonshot, fireworks, xai, ... chat, embeddings and function calling all work. \u0001 — google gemini (chat + embeddings). set \u0001 to your regional endpoint if you use one (e.g. \u0001 is the default). \u0001 — cohere (chat + embeddings + rerank). \u0001 — anthropic claude via the messages api (chat + streaming with extended thinking). thinking blocks stream through the same chain-of-thought channel as openai-compatible reasoning models, so the cot panel works identically. set \u0001 to your anthropic key (\u0001); the default \u0001 is \u0001. extended thinking (optional): set \u0001 (or \u0001) to enable claude's extended thinking. the budget must stay below \u0001 (it is clamped automatically, and floored at 1024 tokens per anthropic's requirement — when \u0001 leaves enough headroom). while thinking is on, \u0001 is omitted from requests — the anthropic api rejects sending them together. anthropic has no embeddings api — to use claude for the llm with remote embeddings, configure an \u0001/\u0001 override (jina, voyage, openai, ...), or keep embeddings local. agent-mode tool calling is not supported yet for anthropic (same as gemini/cohere) — use an openai-compatible provider for the langgraph agent. mixing providers \u0001/\u0001 and \u0001/\u0001 let you mix — e.g. a deepseek llm with jina embeddings: note: deepseek/groq/openrouter do not offer embeddings — use an \u0001 override (jina, voyage, openai, ...) or stay local for embeddings. 4. reranking modes - \u0001 — use cohere rerank when a rerank endpoint is configured, otherwise fall back to embedding-similarity reranking (query embedding vs candidate embeddings, no extra api beyond embeddings). - \u0001 — cohere \u0001 endpoint. - \u0001 — jina \u0001 endpoint (openai-style). - \u0001 — cosine similarity rerank using the configured embedder. - \u0001 — keep retrieval order (no rerank). 5. streaming & chain-of-thought the full sse chat flow (planner → tool call → answer) works over apis. thinking-capable openai-compatible models that stream \u0001 (deepseek-r1, qwen3, ...) feed the same chain-of-thought panel as local thinking models. anthropic extended-thinking \u0001 blocks are mapped to the same \u0001 channel, so claude's deliberation shows up identically in the cot panel. the legacy langgraph agent with human-in-the-loop approval works too, but tool calling is only supported on openai-compatible providers (gemini/cohere/ anthropic raise a clear error for agent mode). 6. checking the mode is active returns which provider/models are active. the api key is never returned. the gpu status endpoint will show nothing loaded in api mode — that is expected (no local models are resident). 7. troubleshooting - \"api request failed (401/403)\" — wrong/missing key; check the config file path is being read (\u0001 in the status endpoint). - \"embedding dim mismatch\" — your embed model's dimension differs from \u0001. set \u0001 (openai \u0001 = 1536, \u0001 = 3072, gemini \u0001 = 768) or leave it \u0001 to auto-detect. - anthropic: \"no embedding model is configured\" — anthropic has no embeddings api. set \u0001/\u0001 (jina, voyage, openai, ...) or keep embeddings local (remove \u0001/\u0001 and rely on bge-m3). - switching modes on an existing project — the chromadb collection keeps the dimension it was built with. re-ingest the project after changing embedding providers. - slow first query — the embedder probes the dimension once at startup; afterwards everything is cached."}, "launcher": {"t": "Setup Launcher", "b": "Setup page internals, provider picker, Ollama automation", "x": "setup launcher a dependency-free launcher that prepares and opens lawyer assistant . users pick one of two paths: - run locally — everything on this computer (private and offline). needs a capable machine: minimum 16 gb ram · 8 gb vram · ~14 gb disk . - use a cloud provider — pick a provider from a dropdown (openai, grok, deepseek, kimi, qwen, mistral, groq, openrouter, together, fireworks, gemini, cohere, claude), paste its api key, and go. parsing and the project index still live on this computer; only model calls (chat/answers and, for providers that offer them, embeddings/reranking) go to the provider. the launcher itself needs only python 3.10+ — no packages to install — so it can run on a fresh machine and do the installing for you. launching the launcher windows: double-click \u0001 macos/linux: \u0001 or from any os: this starts a tiny server on \u0001 (a free port is picked if that one is busy) and opens the setup page in a native electron window — it reuses the electron binary installed for the desktop app, so the setup ui feels like part of the app, not a website. on a truly fresh machine where electron has never been installed, it falls back to your default browser instead (the setup page installs electron as part of the one-time setup). the server is bound to \u0001 only — nothing is exposed to the network. closing the launcher window stops the python launcher server automatically. external links in the launcher (python.org, nodejs.org, ollama.com) always open in the system browser. the launch pad — two buttons, plain words the page is built for non-technical users; there is no setup checklist to walk through by hand. - local card — shows a short guide message (what's missing, or \"everything is ready\") plus exactly two buttons: - ⬇️ install — installs everything in one go (python env, libraries, models, node deps, ollama). it never auto-launches. - 🚀 launch — enabled only when everything is ready (\u0001: python env + deps + all models + node + frontend deps). when it's greyed out the guide message says exactly what to install first. - ⬇️ individual model downloads is tucked behind a collapsed \u0001 for the rare case someone wants to grab one missing model. - api card — a provider dropdown (openai, grok, deepseek, kimi, qwen, mistral, groq, openrouter, together, fireworks, gemini, cohere, claude) with per-provider defaults, an api key field, test connection and 💾 save & launch . no model box to fill in: once a key is pasted, the launcher calls that provider's models endpoint and fills a dropdown with every model the key can actually use (defaulting to a sensible preset), so non-technical users never type a model name by hand. the list refreshes automatically when the key changes and after a successful connection test. saved keys are never echoed back — each saved provider shows only a ✓ badge chip, clickable to reuse that key without re-typing it. the minimums for local mode ( 16 gb ram · 8 gb vram ) are stated in the card subtitle and in the guide message, so nobody starts a 14 gb download on a machine that can't run it. tabs — launch pad and system check the page has two tabs. 🚀 launch pad (the default) is the two-button view above. 🩺 system check holds the detailed hardware report — the readiness score, ram / disk / vram tiles, gpu-vs-cpu model placement and any warnings — so curious users can inspect the machine without cluttering the simple launch view. the report refreshes every time the tab is opened. python environment detection local mode reuses whatever python environment already exists instead of creating a fresh one. detection priority (used everywhere the backend actually runs — state checks, deps check, model downloads and app launch): 1. \u0001 at the repo root (the launcher-created env) 2. \u0001 (classic name) 3. any system python 3.10+ on path when the app is launched, the detected interpreter is forwarded to the electron process as \u0001; the electron bridge (\u0001) and the cli backend runner (\u0001) both honor it, so the backend always runs with the exact environment the launcher detected. the launcher ui shows which one is active (\u0001 / \u0001 / system python). model downloads — per-model the local card lists every model individually with its download status and size, so you can grab exactly what's missing instead of re-running the whole setup: - bge-m3 (embeddings, ≈2.5 gb) → \u0001 - bge-reranker-v2-m3 (≈1.5 gb) → \u0001 - ollama llm (≈5.5 gb pull) — \u0001 each missing model has its own ⬇️ download button (plus a download all missing shortcut). downloads stream live progress into the same log console and progress bar the full setup uses, and only fetch what you asked for. what \"run locally\" does clicking ⬇️ install streams each step live to the log console: 1. python — detects a system python 3.10+. 2. virtual environment — reuses \u0001 or \u0001 if either exists, otherwise creates \u0001 at the repo root. 3. python libraries — \u0001 plus \u0001 (needed for uploads), into the detected environment. 4. ai models — downloads the missing huggingface models only (\u0001 → \u0001, \u0001 → \u0001) — already-present ones are skipped, so a retry after a partial download finishes the remainder quickly. 5. node.js — detects node; the app ui needs it. 6. app dependencies — \u0001 in \u0001 and \u0001. 7. ollama — fully automated: if the cli is missing, the official installer is downloaded and run silently (windows: \u0001; macos/linux: the official install script). the daemon is then started via \u0001 in the background (if it isn't already answering on \u0001) and the launcher waits until the http api responds before moving on. the preferred model (\u0001, override with \u0001) is pulled automatically when absent. non-fatal: search/scan still work without the llm, but the step now installs, starts and pulls by itself instead of asking you to click through an installer. 8. install finishes — it does not launch the app. the user then clicks 🚀 launch (enabled by the fresh state), which calls \u0001 and starts \u0001 (electron) detached, passing the detected python as \u0001; logs go to \u0001. a second setup or model-download request while one is running gets \u0001. failed steps are marked in red and can be retried without redoing the ones that succeeded (already-downloaded models and installed deps are detected and skipped). what \"use a cloud provider\" does 1. pick a provider from the dropdown (or click a ✓ chip for a provider whose key is already saved). 2. paste the api key (e.g. anthropic \u0001, openai \u0001, groq \u0001). the model dropdown is pre-filled with a provider default. 3. test connection — the launcher sends a one-token \u0001 to that provider's api (\u0001 with \u0001) and reports latency. the key is never shown again after entry and never written to logs. 4. 💾 save & launch — validates, writes \u0001 (\u0001, provider set, git-ignored) and opens the app. the backend's api-provider mode (\u0001) handles the provider's chat/answers; embeddings/reranking go to the provider when it offers them, otherwise they stay local (bge-m3 / bge-reranker). a full end-to-end smoke test (\u0001) is still available server-side for advanced debugging. configuration file the launcher reads/writes \u0001 — the same file the backend's api-provider mode reads (see \u0001). it is git-ignored so api keys never get committed. files troubleshooting - \"python is not installed\" — install python 3.10+ from python.org and tick add python to path , then relaunch. - model download fails — check the log console; retry — the helper resumes (already-downloaded files are detected and skipped). - \u0001 fails in \u0001 — often a transient electron binary download issue; retry. the app needs these deps to launch. - ollama missing — the launcher downloads the official installer and runs it silently, then starts \u0001 and waits for \u0001 to respond before pulling the model. if the silent install fails, install manually from ollama.com, then click retry. chat needs ollama; search/scan don't. - ollama server down after install — the launcher auto-starts \u0001 in the background and logs to \u0001 if it can't bind the port. - launcher port busy — a free port is chosen automatically; the browser opens the right one."}, "cli-tools": {"t": "CLI Tools — Command-Line Utilities Reference", "b": "Command-line helpers for ingestion & queries", "x": "cli tools — command-line utilities reference command-line scripts for batch operations, debugging, and automation. all scripts live in \u0001 and are designed for direct python execution (not through the desktop app). --- 1. quick reference script purpose use case -------- --------- ---------- \u0001 bulk document ingestion initial corpus import, re-indexing \u0001 cli search quick retrieval testing \u0001 interactive chat repl cli-based rag queries \u0001 rebuild bm25 index fix corrupted sparse index \u0001 a/b test retrieval compare configs \u0001 legacy benchmark runner compatibility wrapper \u0001 test compliance scanner scan debugging --- 2. \u0001 — bulk document ingestion purpose ingest every document in \u0001 into chromadb + bm25 in one batch operation. usage options flag default meaning ------ --------- --------- \u0001 false wipe chromadb collection before ingesting (full rebuild) \u0001 auto force device (\u0001 \\ \u0001 \\ \u0001) \u0001 32 embedding batch size (lower for small gpus) \u0001 false skip \u0001 (ingest only pdfs/images) examples standard ingestion: full rebuild (wipe + re-ingest): low-vram gpu (4gb): cpu-only: what it does 1. scans \u0001 for supported files (\u0001, \u0001, \u0001, \u0001, \u0001, ...) 2. parses each document with docling (ocr for scanned pdfs) 3. chunks via \u0001 (480 tokens, respect sections) 4. embeds all chunks with bge-m3 (gpu batch processing) 5. stores vectors + metadata in chromadb 6. builds bm25 sparse index from all chunks 7. prints summary: total chunks, files processed, elapsed time output when to use - initial setup — first time indexing your corpus - after adding many files — bulk re-index - after config changes — chunk size, embedding model, etc. - troubleshooting — rebuild corrupted index with \u0001 notes - skips already-indexed files — checks chromadb for existing \u0001 (unless \u0001 is used) - backup index — saves embeddings to \u0001 for crash recovery - memory usage — loads all chunks into ram during embedding (18k chunks ≈ 2gb) --- 3. \u0001 — cli search purpose quick cli search for testing retrieval without the full chat pipeline. usage options flag default meaning ------ --------- --------- \u0001 5 number of results to return \u0001 false skip reranking (faster, lower quality) \u0001 hybrid \u0001 \\ \u0001 \\ \u0001 examples basic search: more results: dense-only search: skip reranking (faster): output when to use - quick retrieval testing — verify indexed documents are searchable - debugging search quality — compare dense vs sparse vs hybrid - benchmarking retrieval speed — measure latency without llm overhead - reranker a/b testing — compare \u0001 vs default --- 4. \u0001 — interactive chat repl purpose interactive command-line chat interface with full rag pipeline (retrieval + llm answer). usage session commands command action --------- -------- \u0001 end session \u0001 end session \u0001 clear screen \u0001 show commands when to use - interactive testing — chat without opening the desktop app - debugging llm answers — see raw retrieval + generation - scripting — pipe queries for automation - remote access — ssh into server, run cli chat notes - requires ollama — llm must be running on \u0001 - uses same pipeline — identical to desktop app rag flow - no conversation history — each query is independent - no approval mechanism — tools execute immediately (no hitl) --- 5. \u0001 — rebuild bm25 index purpose rebuild the bm25 sparse index from existing chromadb chunks (useful when bm25 index is corrupted or missing). usage what it does 1. connects to chromadb (\u0001) 2. fetches all chunks from \u0001 collection 3. tokenizes chunk text (whitespace split) 4. builds bm25okapi index 5. saves to \u0001 output when to use - after chromadb changes — bm25 out of sync with vector store - corrupted bm25 index — error loading \u0001 file - missing bm25 index — \u0001 deleted notes - does not re-embed — uses existing chromadb chunks only - fast — no gpu needed, pure cpu tokenization (~30s for 18k chunks) - safe — does not modify chromadb --- 6. \u0001 — a/b test retrieval purpose compare two retrieval configurations side-by-side on a sample query set. usage what it does 1. loads 20 test queries 2. runs each query through two retrieval configs: - v1: dense-only, no reranking - v2: hybrid (dense+sparse), reranking enabled 3. computes recall@5, mrr for each 4. prints comparison table output when to use - a/b testing retrieval changes — compare before/after config changes - evaluating reranker impact — quantify quality improvement - search mode comparison — dense vs sparse vs hybrid customization edit the script to test your own configs: --- 7. \u0001 — legacy benchmark runner purpose compatibility wrapper for the old benchmark interface (now deprecated; use \u0001 instead). usage equivalent to: see \u0001 for full benchmark documentation. --- 8. \u0001 — test compliance scanner purpose test the playbook compliance scanner on a single document (debugging tool). usage examples scan with default playbook: scan with specific playbook: output when to use - debugging scan failures — see which rules triggered - testing new playbooks — verify rule logic - inspecting clause classification — check confidence scores --- 9. running scripts in production automated ingestion (cron) batch processing ci/cd integration --- 10. common cli workflows workflow 1: add new documents and re-index workflow 2: rebuild corrupted index workflow 3: compare retrieval configs --- 11. cli script troubleshooting issue cause fix ------- ------- ----- \u0001 not in venv activate: \u0001 \u0001 index not created run \u0001 first \u0001 batch too large use \u0001 \u0001 models not downloaded run launcher setup or download manually \u0001 file locked close desktop app first --- 12. related documentation doc coverage ----- ---------- \u0001 full benchmark suite \u0001 dev workflows, debugging \u0001 api & pipeline internals \u0001 ingestion pipeline deep dive \u0001 common failures"}, "security": {"t": "Security — Architecture, Policies & Best Practices", "b": "CSP, Electron sandbox, key handling, threat model", "x": "security — architecture, policies & best practices security architecture and implementation details for lawyer assistant. this document covers the app's security model, content security policy (csp), electron sandboxing, data handling, and security testing. --- 1. security principles local-first by design documents never leave your machine by default. - local mode — 100% on-device. models run on your gpu/cpu. no network calls. - api mode — only the tiny piece of text needed for the answer is sent to the provider. your full files never upload. zero telemetry - no usage tracking - no analytics - no crash reporting to external servers - no phone-home behavior open source - full source code available for audit - no obfuscated code - dependencies are well-known, vetted libraries --- 2. content security policy (csp) the app enforces a strict csp in production to prevent xss, code injection, and unauthorized resource loading. csp rules (production build) what it prevents directive what it blocks ----------- ---------------- \u0001 ❌ inline \u0001 tags<br ❌ \u0001, \u0001<br ❌ external scripts from cdns \u0001 ⚠️ allows inline styles (needed for tailwind)<br ✅ blocks external stylesheets \u0001 ❌ requests to external domains<br ✅ allows backend (8765) + vite hmr (5173) \u0001 ❌ \u0001, \u0001, \u0001 development mode relaxations during development (\u0001), csp allows: - \u0001 — react fast refresh needs inline scripts - \u0001 — vite hmr websocket production builds never allow \u0001 for scripts or \u0001. csp injection (automated) csp is injected into \u0001 during build by a vite plugin: file: \u0001 csp regression guard a ci check (\u0001) fails the build if: 1. \u0001 is missing the csp \u0001 tag 2. production csp contains \u0001 run locally: ci workflow (\u0001): a csp violation breaks the build — the security posture can't regress silently. --- 3. electron security model renderer sandbox the renderer process runs in a strict sandbox with no node.js access: file: \u0001 what sandboxing prevents attack how it's prevented -------- ------------------- arbitrary file access renderer has no \u0001 module access os command execution renderer has no \u0001 access electron api abuse \u0001 isolates apis node.js module loading \u0001 disables \u0001 preload script (controlled bridge) the preload script (\u0001) exposes only safe, controlled ipc: renderer cannot call arbitrary node.js apis — only the pre-defined ipc channels. no \u0001 flag the main process never uses the \u0001 chromium flag (which disables the sandbox entirely). using it anywhere in the codebase breaks the security check. electron security regression guard a ci check (\u0001) fails the build if: 1. any \u0001 doesn't set \u0001, \u0001, and \u0001 explicitly 2. the \u0001 flag appears anywhere in the main process sources run locally: ci workflow: an insecure \u0001 configuration breaks the build . --- 4. api key handling storage api keys (\u0001) are: - ✅ git-ignored — never committed to the repository - ✅ file-permission protected — readable only by the user running the app - ✅ never logged — keys are redacted from all log output - ✅ never echoed back — the \u0001 endpoint returns \u0001, not the real key transmission - keys are sent only to the configured provider's api endpoint (openai, anthropic, etc.) - keys are never sent to any other domain - all api provider communication is over https (tls-encrypted) user control the launcher's api provider card: - ✅ shows saved keys as \u0001 chips (key is never displayed) - ✅ allows deleting a saved key (removes it from \u0001) - ✅ test connection validates the key without persisting it --- 5. file upload security upload constraints constraint limit enforcement ------------ ------- ------------- max files per message 5 frontend pre-check + backend validation max folder files 50 frontend file picker filter scan upload size cap 50 mb backend \u0001 rejects larger supported extensions only \u0001, \u0001, \u0001, \u0001, \u0001, ... backend \u0001 filter path validation uploaded files are saved to the active workspace root — never to arbitrary paths supplied by the user: no path traversal: \u0001 in a filename is sanitized to \u0001. temp file exclusion temp/lock files are never ingested (ignored during workspace scanning): patterns excluded: - \u0001 — word lock files - \u0001 — libreoffice lock files - \u0001, \u0001, \u0001, \u0001, \u0001 — temporary artifacts this prevents indexing transient metadata files that could leak into search results. --- 6. network security backend binds to localhost only the fastapi server binds to \u0001 — not \u0001: the backend is never exposed to the network — only the local electron app can reach it. cors policy cors allows only: - \u0001 (vite dev server) - \u0001 (electron in production) external origins cannot call the api. no external network calls (local mode) in local mode: - ✅ no outbound http requests (models run locally) - ✅ ollama runs on \u0001 (not exposed to network) - ✅ chromadb is a local file database the app is fully functional offline . --- 7. data retention what's stored where data location persistence ------ ---------- ------------- raw uploads \u0001 permanent (user's files) vector index \u0001 permanent (per project) chat history \u0001 permanent (per project) langgraph threads \u0001 permanent (project-independent) compliance flags \u0001 permanent (project-independent) logs \u0001 rotating (10 mb, 5 backups) what's not stored - ❌ api keys are in \u0001 (git-ignored), not committed - ❌ user queries are not logged (only logged locally in rotating log files) - ❌ documents are never uploaded to external servers (local mode) - ❌ telemetry/analytics data (none collected) clearing data per-project data (workspace): global data (conversations, flags): logs : models (14 gb): --- 8. dependency security dependency auditing the project uses well-known, actively maintained dependencies: backend (python): - \u0001 — modern web framework, actively maintained - \u0001 — vector database, chroma team - \u0001 — huggingface, widely used - \u0001 — pytorch, facebook/meta frontend (typescript): - \u0001 — facebook/meta - \u0001 — evan you (vue.js creator) - \u0001 — github/microsoft audit commands: run audits in ci to catch vulnerable dependencies. dependency pinning python dependencies (\u0001) use version pins : not ranges (\u0001) — exact versions for reproducibility. supply chain attacks defense: - all python packages from pypi (official index) - all node packages from npmjs.com (official registry) - no dependencies from git repos or unknown sources - \u0001 / \u0001 lock transitive deps --- 9. secure coding practices input validation all api inputs are validated via pydantic models: invalid input is rejected before processing. sql injection prevention the app uses: - sqlite with parameterized queries (langgraph, flags persistence) - chromadb (vector database, no sql) no raw sql string concatenation anywhere in the codebase. path traversal prevention file operations use safe path resolution : user-supplied paths are never used directly. command injection prevention the app does not execute user-supplied shell commands. the only shell commands are: - \u0001 (hardcoded, no user input) - \u0001 (model name is from config, not user input) - \u0001 (electron spawns backend, hardcoded) xss prevention - csp blocks inline scripts (production) - react escapes by default (jsx auto-escapes) - markdown rendering uses \u0001 (sanitized by default) user input is never inserted into html via \u0001. --- 10. electron-specific security remote module disabled the deprecated \u0001 module is not used — all main↔renderer communication is via ipc. \u0001 enabled \u0001 (default) is never disabled — prevents: - loading insecure content (mixed http/https) - cors bypass - file protocol abuse no \u0001 production builds never set \u0001 (which would allow http content in https pages). native modules sandboxed electron's native modules (\u0001) are not accessible in the renderer due to \u0001 + \u0001. --- 11. security testing automated checks in ci two security checks run on every push/pr: 1. csp validation (\u0001): 2. electron security validation : both checks fail the build on violations. manual security testing test csp enforcement: test electron sandbox: test dependency vulnerabilities: --- 12. threat model in-scope threats ✅ xss via user input — mitigated by csp + react escaping ✅ code injection — mitigated by sandbox + csp ✅ path traversal — mitigated by safe path resolution ✅ api key leakage — mitigated by git-ignore + log redaction ✅ dependency vulnerabilities — mitigated by audits + pinning ✅ electron sandbox escape — mitigated by strict \u0001 out-of-scope threats ⚠️ physical access to machine — user's os-level security responsibility ⚠️ malicious ollama models — user must trust model sources ⚠️ compromised pypi/npm packages — supply chain risk (audits help) ⚠️ os kernel exploits — outside app's control --- 13. incident response reporting a vulnerability email: [your-security-email@example.com] what to include: - description of the vulnerability - steps to reproduce - affected versions - suggested fix (if any) response sla: - acknowledgment within 48 hours - initial assessment within 7 days - fix timeline provided within 14 days disclosure policy - coordinated disclosure — 90 days from report to public disclosure - security fixes released as patch versions (e.g., 1.0.1) - cve assigned if applicable - security advisories published on github --- 14. security checklist for contributors before submitting a pr: - [ ] no api keys, credentials, or secrets in code - [ ] all user input validated via pydantic models - [ ] no raw sql string concatenation - [ ] no user-supplied paths used directly (use safe resolution) - [ ] no \u0001, \u0001, or \u0001 - [ ] no \u0001 in react components - [ ] csp check passes (\u0001) - [ ] electron security check passes (\u0001) - [ ] \u0001 shows no high/critical vulnerabilities - [ ] new dependencies justified and from official registries --- 15. security updates where to watch: - \u0001 - \u0001 — security fixes noted - dependency audit reports (\u0001, \u0001) how to update: --- 16. privacy guarantees local mode ✅ 100% private — no network calls, no data leaves your machine ✅ offline capable — works without internet ✅ no telemetry — zero usage tracking api mode ⚠️ selective data sent — only the small text snippet needed for the answer ✅ documents stay local — full files never uploaded ✅ user controls provider — choose openai, anthropic, etc. ✅ no telemetry — app still doesn't track usage what's never sent - ❌ your full documents - ❌ your file names or directory structure (unless part of a citation in the answer text) - ❌ your workspace folder paths - ❌ your api keys (sent only to the configured provider) - ❌ usage analytics or telemetry --- 17. compliance notes gdpr the app does not collect, process, or transmit personal data — all processing is local (user's machine). api mode sends only text snippets to the chosen provider (user-controlled). hipaa / phi the app is not hipaa-compliant out-of-the-box. healthcare organizations must: - use local mode only (no api providers) - encrypt the workspace folder at rest (os-level encryption) - ensure secure local machine (full disk encryption, access controls) legal documents the app is designed for legal professionals handling confidential client documents . local-first architecture ensures client privilege is maintained. --- 18. related documentation doc coverage ----- ---------- \u0001 system overview, ports \u0001 api endpoints, validation \u0001 ui security, csp \u0001 cloud mode security \u0001 secure development practices \u0001 production security hardening"}, "storage": {"t": "Storage — Database Schemas, Data Models & Persistence", "b": "Where every piece of data lives", "x": "storage — database schemas, data models & persistence complete reference for all data storage in lawyer assistant: database schemas, file formats, data models, and persistence strategies. --- 1. storage overview storage locations data type storage location persistence ----------- --------- ---------- ------------- vector embeddings chromadb \u0001 or \u0001 permanent sparse index bm25 (pickle) \u0001 or \u0001 permanent chat history json \u0001 permanent (per-project) langgraph threads sqlite \u0001 permanent (project-independent) compliance flags sqlite \u0001 permanent (project-independent) logs text file \u0001 rotating (10mb, 5 backups) frontend state localstorage browser storage session (per-project scoped) models pytorch/transformers \u0001, \u0001 permanent temp files filesystem \u0001 temporary workspace vs global storage workspace-scoped (per-project): - vector store (chromadb) - bm25 index - chat history + working set + gpu profile - raw uploads global (shared across projects): - langgraph conversation threads - compliance flags - models - logs --- 2. chromadb schema (vector store) collection structure collection name: \u0001 distance metric: cosine (dot product for l2-normalized vectors) index: hnsw (hierarchical navigable small world) document schema each document in chromadb represents one chunk : metadata fields field type description ------- ------ ------------- \u0001 str source document filename (e.g., \"contract.pdf\") \u0001 int page number in source document (1-indexed) \u0001 str section/heading text (empty if no section) \u0001 str full original paragraph (for sourceviewermodal) chunk id generation properties: - stable (same input → same id) - collision-resistant (sha1) - unique per (file, page, section, text prefix) collection metadata querying chromadb chromadb file structure --- 3. bm25 index schema (sparse retrieval) file format file: \u0001 (python pickle) structure bm25okapi parameters tokenization simple whitespace tokenization: querying bm25 --- 4. workspace history schema (per-project) file format file: \u0001 encoding: utf-8 format: json (pretty-printed, 2-space indent) schema field descriptions conversations: - \u0001: unix timestamp (milliseconds) - \u0001: auto-generated from first user message (max 50 chars) - \u0001: chronological message list - \u0001, \u0001: iso 8601 timestamps messages: - \u0001: \u0001 \u0001 - \u0001: retrieved documents (only for assistant messages) - \u0001: pipeline performance metrics - \u0001: boolean (abstain signal) working set: - pinned/selected sources from conversations - \u0001: user explicitly pinned - \u0001: when added to working set gpu profile: - per-project gpu residency settings - see \u0001 §7 --- 5. langgraph conversations schema (sqlite) database file file: \u0001 engine: sqlite3 schema checkpoints table column type description -------- ------ ------------- \u0001 text conversation id (same as \u0001 in history.json) \u0001 text checkpoint uuid \u0001 text previous checkpoint (null for first) \u0001 blob serialized graph state (messagepack) \u0001 text json metadata \u0001 real unix timestamp (seconds) thread lifecycle 1. new conversation → \u0001 created 2. each tool call → new checkpoint saved 3. human-in-the-loop interrupt → checkpoint persists, thread pauses 4. resume → load checkpoint, continue graph execution 5. conversation ends → checkpoints remain (for future resume) querying conversations --- 6. compliance flags schema (sqlite) database file file: \u0001 engine: sqlite3 schema flags table column type description -------- ------ ------------- \u0001 text uuid (primary key) \u0001 text source document filename \u0001 text chunk id (foreign key to chromadb) \u0001 text section name \u0001 integer page in source document \u0001 text playbook rule id \u0001 text human-readable rule description \u0001 text \u0001 \\ \u0001 \\ \u0001 \u0001 text flagged clause text (excerpt) \u0001 text why the clause was flagged \u0001 real classification confidence (0-1) \u0001 text \u0001 \\ \u0001 \\ \u0001 \\ \u0001 \u0001 real unix timestamp (when flagged) \u0001 real unix timestamp (last status change) \u0001 real unix timestamp (when resolved) \u0001 text user who resolved (optional) flag lifecycle 1. scan document → flags created with \u0001 2. user reviews → status changes: - \u0001 — issue fixed - \u0001 — false positive - \u0001 — needs legal review 3. resolved flags → \u0001 timestamp set querying flags --- 7. frontend localstorage schema storage keys (per-project scoped) frontend uses per-project scoped localstorage keys: key pattern purpose ------------- --------- \u0001 chat state (zustand store) \u0001 pinned sources \u0001 pipeline editor layout (global) hash suffix: sha256 of workspace path → per-project isolation. chat storage schema key: \u0001 zustand persist middleware auto-syncs to localstorage on state changes. working set schema key: \u0001 pipeline layout schema key: \u0001 (global, not per-project) see \u0001 for full schema. --- 8. log file format file location file: \u0001 rotation: 10 mb per file, 5 backups format example: log levels level usage ------- ------- \u0001 verbose diagnostic info (not in production) \u0001 key events (query, retrieval, ingestion) \u0001 recoverable issues (low scores, missing data) \u0001 failures (exceptions, api errors) \u0001 fatal errors (should never happen) parsing logs --- 9. model storage bge-m3 (embedder) location: \u0001 bge-reranker-v2-m3 location: \u0001 model download first run downloads models from huggingface: --- 10. temporary file storage processed directory location: \u0001 or \u0001 contents: - docling temporary images (pdf page images for ocr) - intermediate parsing artifacts - backup embeddings (crash recovery) cleanup: - auto-cleaned on restart (old files removed) - not included in workspace backups --- 11. data migrations chromadb version migration when upgrading chromadb: history.json schema migration add new fields while preserving old data: --- 12. backup strategies workspace backup backup entire workspace (per-project): includes: - chromadb index - bm25 index - chat history + gpu profile - processed temp files global data backup backup global databases: model backup models are re-downloadable — backup not critical: --- 13. storage quotas & limits data typical size max size ------ -------------- ---------- chromadb (18k chunks) ~500 mb unlimited bm25 index ~15 mb unlimited history.json ~1-5 mb unlimited conversations.db ~10-50 mb unlimited flags.db ~1-10 mb unlimited logs (rotating) ~50 mb 50 mb (10mb × 5) models ~14 gb 14 gb temp files ~100 mb auto-cleaned --- 14. related documentation doc coverage ----- ---------- \u0001 workspace storage, per-project isolation \u0001 ingestion pipeline, chunk creation \u0001 api persistence, sqlite operations \u0001 storage path configuration \u0001 storage layout overview"}, "workspace": {"t": "Workspace Management & Auto-Ingestion", "b": "Project folders & the workspace model", "x": "workspace management & auto-ingestion workspace management in lawyer assistant provides per-project isolation with automatic document indexing. when you select a folder, it becomes your active workspace — everything the app saves lives in a dedicated \u0001 sub-folder, and a background watcher auto-ingests new documents you drop in. --- 1. what is a workspace? a workspace is a user-picked project folder that becomes the active context for all operations: - uploads land directly in the folder (raw files stay visible as project documents) - indexes live under \u0001 (\u0001, \u0001, \u0001) - chat history is per-project (\u0001) - gpu settings are per-project (residency profile in \u0001) before workspace system (legacy) everything lived in a static \u0001 directory — one index for all documents, mixed chat history, no project separation. after workspace system (current) each folder is self-contained. point the app at a different folder and you get that folder's index, history, and settings — no cross-contamination. --- 2. selecting a workspace in the desktop app click \"work in a folder\" in the sidebar → a native electron directory dialog opens → pick your project folder. the backend receives the absolute path (no file re-upload needed). via api requirements a workspace is mandatory for: - chat (\u0001, \u0001, \u0001) - file uploads (\u0001) - compliance scans (\u0001) these endpoints return \"no workspace selected. select a project folder first.\" when none is active — there is no default \u0001 folder fallback. --- 3. workspace storage layout when a workspace is active, storage resolves as follows: no workspace active → falls back to project \u0001 directory: workspace override is runtime-only (resets on server restart or \u0001). --- 4. background folder watcher (auto-ingestion) the watcher is a daemon thread that polls the active workspace folder for new documents and auto-ingests them — drop a contract into the folder and it becomes searchable within seconds, with zero user action. how it works key features polling, not fs events - no new dependency (watchdog) - works on all platforms (windows, macos, linux, network drives) - trivially safe to restart - configurable interval (default 8 seconds) cheap skip detection - fingerprint comparison is file metadata only (size + mtime) - when nothing changed, zero chromadb calls - only changed fingerprints trigger ingestion serialization with manual ingestion - \u0001 shared by watcher + sse manual ingest - chromadb writes never race - watcher skips a cycle (non-blocking acquire) when manual ingest is running self-stopping on workspace switch - thread checks \u0001 every cycle - auto-stops if the active workspace changed (handles missed teardowns) status exposed to ui - \u0001 returns live watcher state --- 5. watcher status api returns: fields: - \u0001 — whether the watcher thread is running - \u0001 — the folder being watched - \u0001 — unix timestamp of last scan - \u0001 — how long the scan took - \u0001 — all supported docs discovered - \u0001 — files actually indexed (excludes already-present ones) - \u0001 — total chunks added - \u0001 — already-indexed file count - \u0001 — last exception message (null when healthy) --- 6. manual workspace ingestion trigger a one-time ingestion of every supported document in the workspace: non-streaming (blocking) returns when done: streaming (sse with progress) streams events: force re-index add \u0001 to re-index already-indexed files (rebuilds the entire index): cancel streaming ingestion stops cleanly at the next file boundary (already-completed files stay indexed). --- 7. workspace file listing get discovered documents in the active workspace (ui \"workspace panel\"): returns: files are: - capped at 50 documents (ui limit) - filtered to supported extensions (see \u0001) - excludes \u0001 storage dirs - excludes temp/lock files --- 8. configuration all watcher configuration is in \u0001: constant default meaning ---------- --------- --------- \u0001 \u0001 seconds between folder scans \u0001 (threading.lock) serializes all ingestion (watcher + manual) note: the poll interval is hardcoded, not an env var. to change it, pass \u0001 when constructing \u0001 (or modify \u0001 directly). --- 9. per-project gpu profiles each workspace carries its own gpu residency profile (mode, hot window, max resident, ...) persisted in \u0001 under \u0001. when you switch projects (\u0001), the new project's gpu profile is automatically loaded — settings never leak between projects. see \u0001 §7 for gpu profile details. --- 10. workspace history (chat + working set + gpu) \u0001 stores: - \u0001 — per-project chat history - \u0001 — last-selected conversation - \u0001 — user-starred messages (optional) - \u0001 — pinned sources - \u0001 — per-project gpu residency settings the frontend auto-saves this file via \u0001: read it back: returns: schema: - \u0001 — array of conversation objects (messages, sources, ...) - \u0001 — gpu residency settings (see \u0001 in \u0001) frontend keys (\u0001, \u0001) are scoped per project via a hash suffix — switching workspaces in the ui re-points the localstorage keys, and the server-side \u0001 acts as the durable backup. --- 11. implementation details file responsibility ------ ---------------- \u0001 watcher thread, fingerprinting, auto-ingestion \u0001 workspace root storage, path resolution, gpu profile persistence \u0001 \u0001 endpoints, watcher start/stop \u0001 watcher tests (fingerprint, auto-ingest, lock coordination) watcher lifecycle - started — \u0001 starts the watcher on the selected folder - stopped — \u0001 stops it - self-stops — if \u0001 changes out from under it (leaked thread guard) thread safety - \u0001 protects \u0001 (the snapshot returned by \u0001) - \u0001 serializes chromadb writes (shared with manual ingest) - non-blocking acquire — watcher skips a cycle when manual ingest is running --- 12. troubleshooting symptom cause fix --------- ------- ----- watcher not running no workspace active select a folder via \u0001 new files not indexed watcher stopped or errored check \u0001 → \u0001 files indexed twice manual ingest + watcher both ran this is safe — \u0001 skips already-indexed files watcher slow large corpus, small gpu lower \u0001 or use cpu; watcher respects gpu manager tuning fingerprint false-positive file mtime changed but content same harmless — \u0001 skips re-indexing --- 13. why polling, not fs events? approach pros cons ---------- ------ ------ polling (current) ✅ no new dependency<br ✅ works on network drives<br ✅ cross-platform (no platform-specific quirks)<br ✅ trivially safe to restart ⚠️ 8-second latency (tunable) fs events (watchdog/chokidar) ✅ instant reaction ❌ new dependency<br ❌ platform-specific edge cases<br ❌ network drive issues<br ❌ missed events on restart for a desktop app with human-paced workflows (dropping contracts), 8-second latency is imperceptible — the simplicity/reliability trade-off favors polling. --- 14. future enhancements planned but not yet implemented: - recursive subdirectory support — currently only top-level files are watched - configurable poll interval via env var (\u0001) - watcher pause/resume — ui button to temporarily disable auto-ingest - per-file progress in watcher status — currently only totals - selective file-type watching — watch only pdfs, ignore images, etc. see \u0001 for other planned features. --- 15. related documentation doc what's covered ----- ---------------- \u0001 §2 workspace api endpoints \u0001 system overview, storage layout \u0001 §7 per-project gpu profiles \u0001 ingestion pipeline deep dive \u0001 §4 workspace-related failures"}, "troubleshooting": {"t": "Troubleshooting — Lawyer Assistant", "b": "12 failure scenarios with exact fixes", "x": "troubleshooting — lawyer assistant common failures, their causes, and fixes. cross-referenced with the rest of the knowledge base — start with the quick table, then jump to a section or a doc for depth. getting started? see \u0001 first. --- 0. quick reference symptom likely cause fix --------- -------------- ----- boot screen stuck on \"loading\" backend never became ready §1 — check backend startup / python on path \"could not reach the backend server\" nothing listening on 8765 §1 — start the backend or restart \u0001 \u0001 / port 8765 busy stale backend process §2 — kill the port, restart answers say model \"unavailable\" ollama not running / no model §3 — start ollama, \u0001 \"no workspace selected. select a project folder first.\" no project folder active §4a — pick a folder with \"work in a folder\" (a workspace is required) no sources, \"could not find relevant information\" corpus not ingested §4 — ingest documents (bulk or attach) first query very slow models loading cold §5 — warmup / patience on first run cuda out of memory batch size too large for gpu §6 — \u0001 or cpu electron shows \"frontend dev server failed\" vite can't start on 5173 §7 — \u0001 attached file doesn't appear in chat upload failed / unsupported type §8 — check \u0001, extensions, 5-file cap stream ends with no answer interrupt or sse hiccup §9 — check approvalbar; retry scan fails or flags missing document not indexed / 50mb cap §10 — ingest first, check caps --- 1. backend not reachable on port 8765 symptoms: bootloader stuck on \"loading\"; toast/error \"could not reach the backend server. make sure the python server is running on port 8765.\" causes & checks: if nothing responds, the backend didn't start. common reasons: 1. python 3 not on path — electron's \u0001 needs a system python to create \u0001; if it can't find one it falls back to \u0001/\u0001, which also fails. verify: \u0001. 2. venv/dependency setup failed — the bridge logs to the electron console (\"venv setup failed, falling back to system python\"). fix manually: 3. warmup takes a while — the backend binds the port first, then warms up models in the background, so \u0001 should respond quickly. if the process crashes during warmup (oom, missing model), see §5/§6. fix: restart \u0001 (it kills stale listeners and retries). watch the electron terminal for python stdout/stderr. --- 2. stale processes on 8765 / 5173 symptom: \u0001 errors; a previous instance's backend keeps serving old code. electron already kills listeners on 8765 and 5173 at startup (\u0001 → \u0001). if you still hit conflicts: then restart the app. see \u0001 §3. --- 3. ollama not running / model unavailable symptom: the status/meta line reports model \"unavailable\" ; answers may be generic or missing; \u0001 mode fails. causes: - ollama isn't started, or isn't on \u0001 (the default base url used by \u0001 / \u0001). - ollama is up but the configured model isn't pulled. fixes: retrieval still works without ollama — only llm generation (rag answers, relevance verification, scanning) degrades. see \u0001 §7. --- 4. empty results / \"no relevant documents\" symptom: the agent honestly says it couldn't find relevant information; sources panel is empty; retrieval returns 0 chunks. cause: the corpus was never ingested (empty \u0001 for the active project — or no workspace selected at all). checks: 4a. \"no workspace selected\" symptom: chat/upload/scan error with \"no workspace selected. select a project folder first.\" cause: a project folder is now required — there is no default \u0001 folder fallback. fix: in the app, click \"work in a folder\" and pick the project folder. the backend then stores everything under \u0001. --- 4b. empty workspace index fixes: - in the app: run \"ingest all files\" in the workspace panel (or programmatically \u0001) to index every supported document in the folder at once. - or attach files individually via the + button (or drag & drop) — uploaded files are ingested automatically. - bulk initial ingest of a static corpus (see \u0001 §4): also verify the reranker isn't filtering everything : if scores are all below the threshold, try \u0001 / \u0001. see \u0001 §5. --- 5. slow first query symptom: the first query takes 10–30s; later ones are fast. cause: models (bge-m3, bge-reranker, llm) load on first use. fix: the backend warms up all models in a background thread at startup (\u0001), so in the desktop app the first query is already fast after the warmup completes (~seconds after launch). if you run the backend directly and skip warmup, expect a one-time load. don't kill the app during warmup. --- 6. gpu out of memory symptom: \u0001 during ingestion or embedding. fixes: runtime device is auto-resolved (\u0001 → \u0001 → \u0001) in \u0001 (\u0001); you can override with \u0001 env vars. see \u0001 §5. --- 7. frontend dev server failed symptom: electron window shows \"⚠️ frontend dev server failed\" and hints \u0001. fixes: if 5173 is occupied by another app, stop it or restart \u0001 (which kills stale 5173 listeners). see \u0001 §8. --- 8. attachments don't work symptoms: files don't upload; uploads fail; attached file ignored. checks: - supported extensions only (see \u0001 in \u0001). - max 5 files per message; 50 docs per folder pick. - a project folder must be active before uploading (see §4a); \u0001 saves into the project folder itself; check the electron console for \u0001 logs. - if all uploads fail and you typed nothing, the app bails out with \"no files uploaded.\" see \u0001 §2 and \u0001 §2. --- 9. stream ends without an answer symptoms: answer never arrives; spinner stops; execution log incomplete. causes: - human-in-the-loop interrupt — the agent paused for approval; the approvalbar should appear. approve or reject to resume (\u0001). see \u0001 §4b. - sse hiccup / backend error — the last event was \u0001 or the stream closed. the frontend finalizes with whatever it has; retry the query. - session expired — resuming an old thread returns \"session expired. please start a new conversation.\" (the stored graph was cleaned up). --- 10. playbook scan fails / flags missing symptoms: scan errors; zero flags; \"document not found\". checks: - the document must be indexed (see §4) before \u0001 can fetch its chunks. - 50mb upload cap on scan uploads. - playbooks come from \u0001 — if none listed, the \u0001 may be missing. - flags persist to sqlite (\u0001); if they don't show, re-run the scan and check \u0001. see \u0001 §6. --- 11. diagnostics cheatsheet --- 12. where to look next topic doc ------- ----- running the app \u0001 architecture & ports \u0001 api, agents, retrieval, scan \u0001 frontend streaming & panels \u0001 pipeline editor \u0001 terminology \u0001 agent q&a \u0001"}, "glossary": {"t": "Glossary — Lawyer Assistant", "b": "One-line definitions of every term", "x": "glossary — lawyer assistant one-line definitions for every technical term used in this project, with pointers to the docs that explain them in depth. terms are alphabetical. use this as a companion to \u0001 (q&a) and \u0001 (index). --- a–c - asyncsqlitesaver — langgraph checkpointer that persists agent thread state to \u0001 on disk (falls back to \u0001). note the checkpointer is project-independent — it does not move with the workspace. → \u0001 §4 - bge-m3 — the multilingual embedding model (baai, 1024-dim, l2-normalized) used to turn text chunks and queries into vectors for dense search. → \u0001 §5 - bge-reranker-v2-m3 — the cross-encoder model that re-scores retrieved candidates to pick the best top-5. → \u0001 §5 - bm25 — the classic keyword (sparse) ranking algorithm used for the \u0001 retrieval leg, backed by a prebuilt inverted index in \u0001 when a workspace is active, else \u0001. → \u0001 §5, \u0001 - bypass edge — a temporary particle edge created when a pipeline node is removed, rewiring its incoming sources straight to its outgoing targets so the flow stays connected; tagged \u0001 so re-enabling finds it. → \u0001 §3 - chain-of-thought (thinking) — the llm's intermediate reasoning tokens streamed to the ui (before tools are called) and shown as a collapsible \"thinking\" block. → \u0001 §6 - checkpointer — the langgraph component that stores conversation graph state (threads), enabling human-in-the-loop pause/resume across http requests. → \u0001 §4b - chromadb — the local vector database (collection \u0001, cosine distance, hnsw index) storing chunk embeddings and metadata. with a workspace active it lives in \u0001, else \u0001. → \u0001 §5 - chunk — a 480-token slice of a document (sections never split) that gets embedded and stored; the atomic unit of retrieval. → \u0001 - citation — the source reference (file, page, section) attached to a retrieved chunk and shown inline in answers; clicking opens the sourceviewermodal. → \u0001 §2, §5 - conversation id — the thread identifier passed with every chat request (client-supplied, falling back to a server-generated uuid) so agent state and history follow the conversation. → \u0001 §3 - cors — backend middleware allowing the vite dev origin (\u0001) and \u0001 to call the api. → \u0001 §1 d–h - dense retrieval — semantic search over chromadb embeddings (vector similarity), the \u0001 pipeline node. → \u0001 §5, \u0001 §2 - docling — the document-parsing library (with ocr) used during ingestion to extract text, tables, and structure from pdfs/images. → \u0001 §5, \u0001 - electron — the desktop shell that hosts the app: launches the python backend and vite dev server, owns the window, exposes ipc. → \u0001 §1, §3 - embedding — a 1024-dim numeric vector representing text semantics, produced by bge-m3; cosine similarity between vectors drives dense search. → \u0001 §5 - fastapi — the python web framework serving the app's http + sse api on port 8765 (\u0001). → \u0001 §1 - flag — a compliance finding from the playbook scan (severity, rule id, page, reason, clause text); statuses \u0001 → \u0001/\u0001/ \u0001, persisted to sqlite. → \u0001 §6 - hitl (human-in-the-loop) — the approval step where the langgraph agent pauses via \u0001 before executing a tool and waits for the user to approve/reject; resumed through \u0001. → \u0001 §4b - hnsw — the approximate nearest-neighbor graph index inside chromadb that makes dense search fast. → \u0001 §5 - hybrid search — combining dense (vector) and sparse (bm25) results; the default mode merges them (rrf / boost-only / combine-and-dedup strategies). → \u0001 §5 i–p - mode — the chat request mode: \u0001 (retrieve + answer, default), \u0001 (search only, no llm), or \u0001 (llm only, no retrieval). → \u0001 §2 - ingestion — the pipeline that turns raw files into indexed chunks: parse (docling/ocr) → chunk → embed → store (chromadb + bm25). → \u0001, \u0001 §5 - intent classifier / router — the default rag orchestrator (\u0001): classifies the message intent, writes a short plan, detects the tool from the plan, executes it, and writes the answer. → \u0001 §4a - ipc — electron's inter-process communication used for window controls and backend health/port queries. → \u0001 §1 - langgraph — the orchestration framework powering the agentic router's \u0001 (agent → approval → tools) and checkpointer. → \u0001 §4b - memorysaver — the in-memory fallback checkpointer used when the sqlite checkpointer can't be created. → \u0001 §4b - ollama — the local llm runtime (\u0001) providing generation for rag answers, relevance checks, and scanning. → \u0001 §7 - particle edge — the animated reactflow edge type used throughout the pipeline editor to visualize data flowing between nodes. → \u0001 §1 - playbook — a json rule set (\u0001, e.g. \u0001) defining clause types and compliance rules the scanner checks documents against. → \u0001 §6 - playbook scan — the clause-level compliance check: chunk → classify clause type → check rules → emit flags → persist. → \u0001 §6 r–s - rag (retrieval-augmented generation) — answering a question by first retrieving relevant document chunks, then having the llm write an answer grounded in those chunks with citations. → \u0001 §3, §5 - react — the \"reasoning + acting\" agent loop (legacy \u0001) where the llm decides which tool to call at each step. → \u0001 §4b - reranker — the cross-encoder (bge-reranker-v2-m3) that re-scores the ~50 retrieved candidates and keeps the top 5; can be skipped (\u0001). → \u0001 §5 - rrf (reciprocal rank fusion) — the rank-based merge strategy used in hybrid search to combine dense and sparse rankings. → \u0001 §5 - sparse retrieval — bm25 keyword search over the inverted index; the \u0001 pipeline node. → \u0001 §5 - sse (server-sent events) — the \u0001 protocol the backend uses to stream chat tokens, statuses, tool calls, node progress, and scan progress to the ui in real time. → \u0001 §3, \u0001 §2 - streaming event — one typed sse payload (e.g. \u0001, \u0001, \u0001, \u0001, \u0001); the frontend switches on these types. → \u0001 §3 t–z - thread / thread id — the langgraph conversation thread key (same as the conversation id) used to pause, store, and resume agent state. → \u0001 §4b - tool — a function the agent can call: \u0001, \u0001, \u0001, \u0001. → \u0001 §4c - top-k — how many results are requested/returned: 50 candidates retrieved, 5 returned after reranking (\u0001). → \u0001 §5 - upload — saving an attached file into the active project folder via \u0001 so it can be ingested/scanned with the next message (a workspace must be selected first). → \u0001 §2 - vector store — chromadb's persistent storage of chunk embeddings + metadata (file, page, section) used for dense retrieval. → \u0001 §5 - warmup — the background thread at backend startup that preloads the embedder, reranker, llm, and classifier references so the first query is fast. → \u0001 §1 - working set — the pinned/selected sources panel that collects documents of interest during a session. → \u0001 §5 - zustand — the lightweight react state store persisting conversations (\u0001) and holding all streaming/approval state. → \u0001 §3"}, "agent-knowledge-base": {"t": "Agent Knowledge Base — answering questions about this app", "b": "Fast Q&A for AI agents & developers", "x": "agent knowledge base — answering questions about this app this is the fast-answer reference for humans and ai agents. each entry gives a concise answer plus the doc to read for depth. if an answer here conflicts with the root \u0001, trust this document (and the docs it references) — the root readme is the non-technical user guide; these docs are the source-verified technical reference. --- q&a what is this app? a local-first legal research desktop application (\"lawyer assistant\") for legal documents. the ui, prompts and ocr are english, and retrieval is language-agnostic (the bge-m3 embedding model handles many languages, so non-english source text can still be searched). it searches a legal corpus with bge-m3 embeddings + chromadb + bm25 + a cross-encoder reranker , answers questions with a local ollama llm (rag with citations), and runs a compliance playbook scan that flags risky clauses in documents. it is an electron app with a react frontend and a python fastapi backend. → \u0001. what are the three \"modes\" of chat? \u0001 (default; retrieve + answer), \u0001 (search only, no llm), \u0001 (llm only, no retrieval). → \u0001 §2. what ai \"agents\" does the app have? - intent router (\u0001) — the default rag orchestrator: classifies intent, writes a plan, picks tools from the plan, executes them, writes the answer. - legacy agentic router (\u0001) — a langgraph react agent (agent → approval → tools → loop) with human-in-the-loop approval. - tools (the agent's \"hands\"): \u0001, \u0001, \u0001, \u0001. - playbook scanner — a clause classifier + rule engine (not an llm agent, but an ai component using embeddings). → \u0001 §4, §6. what does the agent do with my query? rules enforced by prompts: the exact user query is passed to \u0001 without rephrasing , results are verified with \u0001, and the final answer is written from retrieved chunks with file/page citations — or it says honestly that documents don't contain the answer. → \u0001 §4b. what is human-in-the-loop approval? before the agent executes a tool, the langgraph graph pauses with \u0001. the ui shows an approvalbar; the user approves or rejects; \u0001 resumes the thread with \u0001. reject makes the agent answer without tools. thread state persists in \u0001 (asyncsqlitesaver). → \u0001 §4b, §3. how does retrieval work? query → bge-m3 embedding → dense search (chromadb, top 50) + sparse (bm25, top 50) → hybrid merge → bge-reranker-v2-m3 cross-encoder re-scores → top 5 results with scores, file/page/section metadata. → \u0001 §5. how does document ingestion work? files are saved into the active project folder (a workspace must be selected first — uploads land directly in that folder), parsed with docling (ocr for scanned pdfs), chunked by the structure-aware chunker (480-token chunks, sections never split), embedded with bge-m3, stored in chromadb, and indexed for bm25. → \u0001, \u0001 §5. how does the compliance scan work? document chunks → classify clause type (embedding classifier) → check each playbook rule → emit flags (severity high/medium/low, rule id, page, reason, clause text) → persist to sqlite → ui lets you resolve / dismiss / escalate. playbooks are json files in \u0001. → \u0001 §6. how do attachments work? attach up to 5 files (or a folder of up to 50 docs) → each is uploaded to \u0001 (saved into the active project folder — a workspace must be selected first) → the paths ride along on the chat request → the router ingests (\u0001) and/or scans (\u0001) them; if you attach files without typing, it runs a default batch-scan prompt. → \u0001 §2, \u0001 §2. what does \"select a folder\" do? (workspace) the \"work in a folder\" button opens a native electron directory dialog and the picked folder becomes the active workspace — and a workspace is now required : chat, upload and scan all refuse to run without one (there is no default \u0001 folder anymore). raw uploads land directly in that folder, and the chromadb/bm25 indexes, processed temp files and per-project history all live under \u0001 (\u0001, \u0001, \u0001, \u0001), replacing the static project \u0001 paths. \u0001 sets it, \u0001 reads it, \u0001 resets it, and \u0001 (or its sse variant \u0001) indexes every supported document in the folder. a background folder-watcher auto-ingests new files dropped into the project. the override is runtime-only (resets on restart). the frontend passes workspace file absolute paths on send (no re-upload). → \u0001 §2 (workspace), \u0001 workspace section. what ports does the app use? 8765 backend (fastapi), 5173 vite dev server, 11434 ollama. electron kills stale 8765/5173 processes at startup. → \u0001 §2. where is data stored? - browser: conversations and working set in per-project scoped localstorage keys (\u0001, \u0001), pipeline layout (\u0001). - disk, project folder: \u0001 — \u0001 (vectors), \u0001 (sparse index), \u0001 (temp artifacts) and \u0001 (per-project chat history + working set + gpu profile). raw uploads live directly in the project folder itself. - disk, project-independent: \u0001 (langgraph agent threads), \u0001 (scan flags). - models: \u0001, \u0001. → \u0001 §5. how does streaming work? the backend streams server-sent events (\u0001): tokens, thinking (chain-of-thought), statuses, the plan, tool calls/results, node start/node complete, sources, interrupted/resumed, done, error. the frontend parses sse lines and appends to zustand streaming state. workspace ingest streams its own event set (\u0001, \u0001, \u0001, \u0001, \u0001). → \u0001 §3, \u0001 §2, §6. what is the pipeline editor? a reactflow canvas visualizing the pipeline as nodes (intent, planner, dense, sparse, rerank, ingest, scan, answer). nodes can be removed (auto-bypass rewires the flow), re-enabled via palette toggles, and the whole layout is auto-saved, resettable, and exportable/importable as json. since the \u0001 endpoint, the layout also reconfigures the python backend at runtime (rerank on/off from the \u0001 node, search mode + fusion weights from the dense/sparse nodes, \u0001 overrides). the config is runtime-only and resets on restart. → \u0001, \u0001 §10. why is the ui light by default? / how does theming work? the app defaults to a light \"parchment\" theme; \u0001 + css variables drive all colors, and a bridge remaps tailwind utility classes so text never matches the background in either theme. → \u0001 §7. how do i run the app? \u0001 at the root (runs electron, which boots the fastapi backend and vite itself). non-technical users: double-click \u0001 / \u0001 to open the launcher, which installs everything and starts the app. for production, \u0001 then \u0001 (electron-builder). electron auto-creates/uses \u0001 for the backend. → \u0001 §3. why is the first query slow? models are preloaded at backend startup (warmup thread), so after startup the first query is fast; without warmup the first call pays model-loading cost. cli (\u0001) also loads models on first use. what are the tests/benchmarks?\u0001 has phase-based suites (boot, types, chunking, embeddings, vector store, retrieval, integration e2e); \u0001 has component tests and golden eval harnesses; cli \u0001 runs them. → \u0001, \u0001 source. --- how to answer accurately (for agents) 1. scope check: is the question about the desktop app (electron/react/ fastapi) or the legacy cli (\u0001)? point to \u0001 for the former; the cli is not user-facing anymore. 2. prefer code over memory: verify specifics against \u0001, \u0001, \u0001 when the question is technical. 3. pipeline editor = runtime config: since \u0001, the editor's layout does reconfigure retrieval at runtime (rerank on/off, search mode + fusion weights, top k) — it is not visual-only. say so accurately (see \u0001 §10). 4. cite the doc: end answers with the relevant doc filename so the questioner can go deeper."}, "changelog": {"t": "Changelog — Lawyer Assistant", "b": "Recent feature history", "x": "changelog — lawyer assistant recent feature history, derived from the git history and the current working tree. this document exists so agents (and humans) can answer \"what changed recently?\" without digging through \u0001. conventions: date groups are newest-first; within a group, commits are listed oldest-first (chronological). items marked unreleased (working tree) are implemented but not yet committed. cross-references go to the \u0001. --- unreleased (working tree) work completed on top of the latest commit (\u0001). mostly the documentation set and the pipeline-editor feature pack . documentation set (this session) - full knowledge base added under \u0001: \u0001 (index), \u0001, \u0001, \u0001, \u0001, \u0001, \u0001, \u0001, \u0001, and this \u0001 — every doc fact-checked against the source. - \u0001 at the repo root: read order, topic → source-file table, known traps, and answering etiquette for ai agents. - root \u0001 rewritten as the current non-technical user guide (install + use via the launcher), replacing the old cli-era content. - docs aligned with the unified repo layout — every structure reference in \u0001 now matches the current tree: the whole python side under \u0001, the whole frontend + electron shell under \u0001, plus the \u0001 (setup page), \u0001 (screenshots) and \u0001. stale cli/flask-era path claims removed. pipeline editor feature pack - sub-node split : the search documents and scan document nodes were decomposed into sub-nodes (\u0001, \u0001, \u0001, \u0001, \u0001) so users can remove individual stages (e.g. disable reranking). - bypass removal : removing a sub-node auto-wires a tagged bypass edge (\u0001) so the remaining nodes still connect. - per-node toggle re-enable : re-enable a removed sub-node from the palette without clearing the layout; the original edges are rewired automatically. - persistence : node/edge layout saved to \u0001 (\u0001) with debounced saves, so removals/edges survive reloads; a reset to default button restores the original flow. - export/import : the layout can be exported/imported as a json file (versioned format) to share retrieval configurations across machines. - zero-node guard : validation prevents committing an empty pipeline. - backend runtime config : \u0001 applies the layout json to retrieval at runtime — rerank on/off from the \u0001 node, \u0001 (hybrid/dense/sparse) + fusion weights from the dense/sparse nodes, and \u0001/\u0001/weight overrides. runtime-only (resets on restart); \u0001 reads it, \u0001 resets. see \u0001 §10. workspace folders (working tree) - select a folder = select the workspace. the \"work in a folder\" button now opens a native electron directory dialog (\u0001 ipc, absolute path) instead of only reading the folder name from a \u0001 input. - new backend workspace api : \u0001 and \u0001. setting a workspace re-points storage — raw uploads land directly in the folder, and \u0001/\u0001/\u0001 live under it — so search & ingestion are isolated per workspace instead of the static project \u0001. - frontend wiring : \u0001 sets the workspace on pick, refreshes the discovered file list, and passes workspace file absolute paths on send (no byte re-upload); \u0001 shows a workspace card with an ingest all files action. browser fallback (no electron bridge) keeps the old \u0001 behavior. - runtime-only: the override resets on restart / delete. ui fixes (this session) - chat input container height reduced ~50%. - attachment (+) button wired to the upload backend, and drag-and-drop of files onto the composer added (committed as \u0001). citation integrity & retrieval warnings (working tree) - citation schema validation (\u0001 in \u0001, applied on both routers' done events and in \u0001/\u0001): a citation marker must be an integer in \u0001. malformed markers like \u0001 (no such source) and \u0001 / \u0001 / \u0001 (in-document section numbers leaking into citation output) are stripped before reaching the ui. - citation content verification : each \u0001 is checked against the cited chunk's actual text — the chunk must share enough of the claim's distinctive content words (lexical-entailment heuristic). a chunk that does not support the sentence loses its citation marker, so one top-ranked chunk can no longer be cited for five unrelated clause types. - answer prompt hardened (\u0001 \"answer\" + \u0001 system prompt): explicit rules against leaking section numbers into \u0001 markers, against reusing one source for unrelated clauses, and a document-scope rule — when results span multiple unrelated companies/entities, section the answer by document instead of blending into one false \"this agreement\" narrative, and state the ambiguity. - retrieval-quality caveats (\u0001): done events now carry \u0001 when the top score is below 70% (weak match) or results span multiple documents; the ui renders it as a visible banner above the answer (client-side fallback for legacy messages). answer-transparency & trust fixes (this session) - abstain signal shipped end-to-end : the backend now emits \u0001 on the done event (both routers + \u0001 response) whenever the pipeline abstains. the ui trusts this signal and: - hides the confidence percentage bar and the real citation chips on abstained answers, - shows a muted \"closest match found (not used)\" row instead of a real citation when chunks were retrieved but rejected, - shows the three-state \u0001 (found / partial / not found) instead of a raw percentage on real answers. - step counts moved to a debug view : \"process (27 steps, 0 tools)\" and \"pipeline (n steps)\" labels no longer appear in the default user-facing view; step/tool counts live inside the collapsible process section only. - temp/lock files filtered (\u0001 word locks, \u0001 libreoffice locks, \u0001): excluded from directory ingestion (\u0001, \u0001), from \u0001 (retroactive — already-indexed junk no longer lists), and from the workspace file listing. security hardening & ci (this session) - content-security-policy injected into the renderer html by a vite plugin (\u0001 in \u0001): production ships \u0001 (no inline scripts, no \u0001), development allows \u0001 + the hmr websocket — both modes silence electron's insecure-csp warning. - \u0001 warning fixed — the folder-picker input passes the chrome-only attribute as a string with a proper \u0001 module type augmentation instead of \u0001. - csp regression guard in ci — \u0001 fails the build (exit 1) when \u0001 is missing its csp meta tag or any policy contains \u0001. wired into \u0001 (frontend self-check) and added as a standalone \u0001 script + a github actions workflow (\u0001) that installs, builds, and verifies the csp on push/pr — the security posture can't regress silently. - electron renderer security guard — \u0001 statically scans the main-process sources (\u0001) and fails the build when any \u0001 does not set \u0001, \u0001, and \u0001 explicitly (or uses an unverifiable spread/variable \u0001), or when the \u0001 command-line switch is used anywhere. \u0001 now sets \u0001 explicitly. wired into \u0001 (electron self-check), a \u0001 entry, and a dedicated \u0001 job in the ci workflow — csp covers what the renderer may load, these flags cover what it can do. --- 2026-07-31 — light mode & drag-and-drop commit change -------- -------- \u0001 light/dark mode fixes — hard-coded colors corrected so text never matches its background \u0001 drag-and-drop file upload — drop documents onto the chat input area (in addition to the picker) \u0001 default to light mode — app opens in light mode; \u0001 class bridged with the theme store 2026-07-30 — pipeline editor, chain of thought, retrieval tuning commit change -------- -------- \u0001 pipeline editor — custom nodes added; nodes can be removed on the canvas \u0001 chain of thought — agent reasoning steps surfaced to the user \u0001 procedure visible — the agent's procedure persists and is shown to the user \u0001 ranking optimized \u0001 playbook and agent combined — compliance scanning integrated with the agent loop \u0001 router logic enhanced \u0001 full paragraph save to chromadb — paragraphs stored as retrieval units \u0001 ui improved with more confidentiality terms \u0001 ui enhanced \u0001 animation enhanced 2026-07-29 — streaming, approval dashboard commit change -------- -------- \u0001 ui enhanced \u0001 loop state mismatch solved \u0001 approval & processing dashboard added (human-in-the-loop ui) \u0001 streaming reply added — sse token streaming to the chat 2026-07-28 — pipeline loop & centralized prompting commit change -------- -------- \u0001 dense + sparse results mixed before reranking \u0001 centralized prompt — pipeline restructured with verify / generate / post-generate nodes \u0001 pipeline loop added + ui 2026-07-27 — benchmarking commit change -------- -------- \u0001 benchmarking added to the docs (\u0001 harness, golden eval) 2026-07-26 — initial release commit change -------- -------- \u0001 first commit — project scaffold --- how to keep this file accurate - when a new feature ships, add an entry at the top (unreleased → dated block on commit). - if you are an agent answering \"what changed recently?\", read this file first, then verify against \u0001 and the feature docs linked above. - deep dives: \u0001 (editor), \u0001 (engine), \u0001 (ui), \u0001 (known failures)."}, "readme": {"t": "📚 Documentation — Lawyer Assistant", "b": "The documentation index itself", "x": "📚 documentation — lawyer assistant everything you need to build, debug, or understand the app — organized so you find answers fast. official repository: \u0001 · issues & prs welcome. --- 🔥 start here i want to... read this --- --- use the app \u0001 — from clone to first query fix something broken \u0001 — 12 failure scenarios with exact fixes understand the architecture \u0001 — system diagram, ports, runtime modes --- 📖 full index document what's inside who it's for --- --- --- 🚀 \u0001 clone → install → run → first query → verify new developers 🩺 \u0001 backend down, ollama missing, stale ports, empty results, gpu oom, slow first query anyone debugging 📋 \u0001 recent feature history — answers \"what changed recently?\" everyone tracking progress 🏗️ \u0001 system overview, electron/vite/fastapi components, ports, runtime modes high-level understanding 🐍 \u0001 fastapi endpoints, ai routers, tools, retrieval pipeline, compliance scan, config api & backend questions ⚛️ \u0001 react layout, chat flow, zustand stores, streaming events, theming, api layer ui/ux & state management 🎨 \u0001 visual canvas: sub-nodes, toggles, bypass edges, persistence, export/import pipeline feature 📐 \u0001 exact json schema — write a valid layout from scratch ai models & hand-editors 🖥️ \u0001 smart vram sharing, ram-first residency, priority, batch auto-tuning, per-project profiles gpu tuning & low-end hardware 🤖 \u0001 quick q&a — the fastest path to a correct answer ai agents answering questions 📖 \u0001 one-line definitions for every term (rag, hnsw, bm25, sse, hitl, …) terminology lookup 🔍 \u0001 step-by-step: user types message → intent → tools → rerank → llm → answer deep-dive: chat request end-to-end 📊 \u0001 document ingestion: parse → chunk → embed → chromadb + bm25 deep-dive: data pipeline 🗺️ \u0001 planned improvements to the rag pipeline roadmap 🎨 \u0001 cloud mode: providers, env vars, mixing providers, rerank modes, extended thinking api mode setup 🚀 \u0001 setup page internals: install steps, provider picker, ollama automation launcher development --- 🗺️ quick topic map --- 🤖 for ai agents 1. start with \u0001 for the fastest correct answer. 2. follow cross-references to \u0001, \u0001, \u0001 for depth. 3. verify facts against source code: \u0001, \u0001, \u0001. --- 📁 project layout"}, "ui-brainstorm": {"t": "🏛️ Making It Smooth & Trustworthy for Lawyers", "b": "Design explorations & future UI ideas", "x": "🏛️ making it smooth & trustworthy for lawyers 🔴 trust builders (immediate impact) 1. confidence meter on every answer - add a confidence bar below each answer (high / medium / low) - based on reranker's top score + number of matching sources - color-code: 🟢 high (score 0.7, 3+ sources), 🟡 medium (0.4-0.7), 🔴 low (< 0.4) - label: \"based on 5 documents, confidence: high (87%)\" 2. citation inline formatting - render clickable \u0001 superscripts inline in answer text, like legal briefs - when clicked, open sourceviewermodal or scroll to source card - standard format for westlaw, lexisnexis, casetext 3. \"why this answer\" transparency box - collapsible box explaining how the system arrived at the answer - shows: chunk count, reranker score, source documents used - creates audit trail for court-defensible research 4. \"not in documents\" reframed - change message to: \"this specific question wasn't addressed in your indexed documents.\" - add: 📋 \"consider adding documents that address [topic]\" - reframes absence as actionable intelligence 🟡 smoothness (reduces friction) 5. progressive answer rendering - semantic chunks: direct answer → supporting evidence → caveats - each section slides in with fadeslideup animation - mirrors how lawyers read: answer first, then authority 6. keyboard-first navigation - \u0001 navigate source cards, \u0001 command palette - \u0001 close modals, \u0001 switch panels - number keys \u0001 open source citations 7. contextual loading states - \"🧠 analyzing your question...\" - \"🔍 searching 18,836 document chunks...\" - \"📊 reranking top 100 candidates...\" - \"✍️ drafting response with citations...\" 8. smart auto-scroll - auto-scroll during streaming, but stop if user scrolls up - show \"↓ new content\" pill at bottom 🟢 professionalism 9. document header in source viewer 10. dark/light mode toggle - light mode: cream background, dark text (feels like paper) - dark mode: current design (feels like terminal) - persist in localstorage 11. answer length controls - brief: 1-2 sentences - standard: paragraph with citations - detailed: full analysis with all supporting passages 12. export as legal memo 🔵 trust & compliance 13. \"local processing\" badge - persistent: 🔒 \"all data processed locally — nothing sent to external servers\" - massive differentiator vs. chatgpt/claude 14. professional disclaimer \"ai-generated responses based on your indexed documents. always verify citations against source documents. this tool does not provide legal advice.\" 15. audit log - history tab showing every question, sources cited, confidence level - creates defensible record of research process 🟣 micro-interactions 16. source card hover effects - 2-line text preview on hover - subtle shadow lift with translatey(-1px) - relevance bar animates wider 17. staggered answer reveal - 0ms: answer text fades in - 200ms: confidence meter slides in - 400ms: source buttons appear one by one 18. input bar enhancements - subtle character count - file attachment chips above input - rotating placeholder prompts 19. empty state upgrade - legal scales icon - corpus stats: \"18,836 passages from 511 documents\" - subtitle: \"every answer is cited to source text\" 🏗️ architecture suggestions 20. answer pre-processing - check source count before llm call - 0 matches → show \"not found\" immediately - low confidence → prepend caveat 21. source deduplication - group multiple chunks from same file - show as expandable tree: \u0001 22. skeleton loading - pulsing gray lines in answer shape while llm thinks - reduces perceived wait time by ~40% priority matrix priority item effort impact ---------- ------ -------- -------- 🔴 p0 confidence meter low very high 🔴 p0 inline citations [1] medium very high 🔴 p0 local processing badge trivial high 🔴 p0 professional disclaimer trivial high 🟡 p1 \"why this answer\" box medium high 🟡 p1 export as legal memo medium high 🟡 p1 progressive answer rendering medium high 🟡 p1 keyboard navigation medium high 🟢 p2 source grouping medium medium 🟢 p2 skeleton loading low medium 🟢 p2 dark/light mode medium medium 🟢 p2 audit log high medium"}, "chatbot-ui-design": {"t": "Chatbot UI Design Recommendation for Lawyer Assistant", "b": "Chat UI design notes", "x": "chatbot ui design recommendation for lawyer assistant overview this document presents a comprehensive design recommendation for building a modern, professional electron desktop application for the legal document retrieval system. the interface will provide an intuitive way for users to interact with the rag (retrieval-augmented generation) pipeline, ask legal questions, and receive answers with source citations. architecture overview key design principles 1. professional & trustworthy - legal domain requires a clean, authoritative design 2. clean & modern - contemporary design with clear visual hierarchy 3. information-rich - display sources, citations, and document references prominently 4. responsive - works seamlessly on desktop, tablet, and mobile 5. accessible - wcag 2.1 compliance for broad usability 6. offline-capable - core functionality works without internet technology stack frontend (renderer process) - react 18 + typescript - ui components - vite - fast build tooling - tailwind css - styling - zustand - state management - shadcn/ui - ui component library electron main process - electron-vite - build tool for electron + vite - electron-builder - packaging and distribution - electron-updater - auto-updates backend (python) - fastapi or flask - local http server - chatpipeline - existing rag implementation - uvicorn - asgi server project structure layout design desktop layout source viewer modal (floating) component breakdown 1. custom title bar - custom window controls (minimize, maximize, close) - drag region for window movement - app logo and title 2. chat area component message components: - usermessage - right-aligned, primary color background - assistantmessage - left-aligned, neutral background with: - markdown rendering - inline citation highlights (clickable [1], [2]) - code blocks - typingindicator - animated dots with \"thinking...\" 3. source viewer modal - floating draggable window - full source paragraph with highlighted terms - document metadata (filename, page, section) - actions : copy, pin, open document - multiple viewers can be open simultaneously 4. sources panel - source list with citation badges - \"view full source\" button on each - visual highlighting synced with chat electron main process window management python backend bridge preload script (security bridge) ipc communication main process handlers frontend api service python backend api packaging & distribution electron-builder.yml build scripts styling & theming color palette (light mode) color palette (dark mode) typography interactive features 1. citation interactions - hover - tooltip with source preview - click - opens source viewer modal - visual sync between chat citations and sources panel 2. source viewer modal - draggable, resizable, pinnable - multiple viewers simultaneously - full paragraph with context - query term highlighting 3. keyboard shortcuts - \u0001 - new chat - \u0001 - close current chat - \u0001 - settings - \u0001 - toggle sources panel - \u0001 - close modals security considerations 1. context isolation - enabled by default 2. node integration - disabled in renderer 3. preload script - secure bridge only 4. csp headers - strict content security policy 5. input sanitization - all ipc data validated 6. local backend - no external network access required implementation phases phase 1: project setup - initialize electron + vite + react project - set up python backend with fastapi - configure ipc communication - create basic window with custom title bar phase 2: core chat - implement chat interface - message display (user/assistant) - api integration with chatpipeline - basic error handling phase 3: sources & citations - sources panel - citation highlighting - source viewer modal - document preview phase 4: history & settings - conversation history sidebar - settings modal - theme switching (light/dark) phase 5: polish & packaging - animations & transitions - keyboard shortcuts - auto-updater - platform packaging (windows, mac, linux) estimated timeline - week 1-2 : project setup, electron + python integration - week 3-4 : core chat, api integration - week 5-6 : sources, citations, source viewer modal - week 7-8 : history, settings, theming - week 9-10 : polish, packaging, testing dependencies frontend electron backend --- this design document provides a comprehensive roadmap for building a professional, secure, and feature-rich electron desktop application for the lawyer assistant system."}}